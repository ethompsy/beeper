apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-backend
  namespace: beeper-demo
  labels:
    app: demo-backend
    app.kubernetes.io/part-of: beeper-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-backend
  template:
    metadata:
      labels:
        app: demo-backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: backend
          image: beeper/demo-app:latest
          ports:
            - containerPort: 8081
          env:
            - name: SERVICE_ROLE
              value: "backend"
            - name: SERVICE_PORT
              value: "8081"
            - name: FAULT_ENABLED
              value: "false"
            - name: FAULT_TYPE
              value: "none"
            - name: SYNTHETIC_TRAFFIC
              value: "true"
            - name: DATABASE_URL
              value: "http://demo-database:8082"
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            limits:
              cpu: 200m
              memory: 128Mi
            requests:
              cpu: 50m
              memory: 64Mi
---
apiVersion: v1
kind: Service
metadata:
  name: demo-backend
  namespace: beeper-demo
  labels:
    app: demo-backend
    app.kubernetes.io/part-of: beeper-demo
spec:
  selector:
    app: demo-backend
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
