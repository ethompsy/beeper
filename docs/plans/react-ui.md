# Implementation Plan: Beeper — SRE-Centric React UI Overhaul

## Overview

Incremental React overhaul of the Beeper UI, incident-triage surface first, delivering a first-seconds triage glance, full permalinks, and a `/design-sync`-ready component library — without a big-bang rewrite. Traces to the "SRE-Centric React UI Overhaul" section of [docs/reqs/main.md](../reqs/main.md) (FR45–FR53, NFR19–NFR24, R1–R6). The completed pipeline/UI plan ([docs/plans/main.md](main.md)) is untouched; this is a separate, forward-looking workstream (Expansion / v0.2.0).

**Definition of Done (first increment):** the investigation list + detail run in React at parity with their Jinja equivalents, are deep-linkable permalinks, and the extracted component library passes a trial `/design-sync` run — with a witnessed triage-glance test passing.

> **Baseline corrections (found in planning review, verified against code):** (1) the operator exposes **no native SSE** — the current `investigations/<id>/stream` endpoint is a **BFF-side** loop that polls the operator REST every 3 s and yields **rendered HTML** for HTMX swap ([investigations.py](../../ui/beeper_ui/routes/investigations.py)); React needs a JSON stream (D10). (2) The UI is **not** unauthenticated — `ui/beeper_ui/middleware/permissions.py` implements a two-tier RBAC and `ui/beeper_ui/websocket/` runs a Flask-SocketIO collaboration layer (D12). `docs/specs/architecture.md`'s "no auth / SSE only" is inaccurate and should be corrected (Q7).

## Decisions

| # | Decision | Context | Rationale |
|---|----------|---------|-----------|
| D1 | New plan file `docs/plans/react-ui.md`; scope = React workstream (FR45–53, NFR19–24) | Completed `main.md` is delivered | Preserve history; plan only net-new work |
| D2 | Vite + React + TypeScript | Need a static bundle the Flask BFF serves | Simplest fit for a BFF-served SPA bundle; pairs with Vitest/Playwright/Storybook (Next.js rejected — own server conflicts with BFF) |
| D3 | shadcn-style library (Radix + Tailwind, copied in), skinned to the dark-first tokens | R1 — user wants flexible + Claude-Design-friendly | Headless/accessible, no lock-in, token-skinnable, clean `dist` bundle for `/design-sync` |
| D4 | Flask BFF hosting; React shell served on React-owned paths | R2 resolved | Reuses the existing proxy process; no operator CORS/auth change |
| D5 | Incremental migration, incident-triage first; coexist; retire Jinja per inventory | Atomic rewrite is highest-risk | Fast path to value + design-sync; bounded risk |
| D6 | Permalinks encode content state in URL; list filter maps to `list_investigations(status=…)` | R6 — service+operator already accept `status`/`service`/`severity` ([investigation_service.py](../../ui/beeper_ui/services/investigation_service.py)) | No operator change (filter is post-list at demo scale) |
| D7 | "Affected component" derived best-effort from `condition`/anomaly signal; NOT in the 5-s triage bar yet | R5 — Investigation CRD has no `component` field ([investigation.rs](../../operator/src/crds/investigation.rs)) | Unblocks the glance; dedicated field is a later operator change (Q2) |
| D8 | Tests: Vitest + RTL (unit/component), Playwright (e2e: permalinks, triage, scroll, reduced-motion), Storybook (design-sync source) | Net-new frontend | Standard, CI-friendly |
| D9 | Blast radius (FR48) out of first increment | Gated on RFC 0001 Phase 3 (not delivered) | Operator emits no correlation fields; Phase 3 below |
| D10 | React consumes a **new BFF JSON event endpoint** (`/api/v1/investigations/{id}/events`) that reuses the existing operator-polling loop but emits JSON `{order, step, status}` | Existing stream emits HTML-for-HTMX; operator has no native SSE | React cannot render HTML partials; "reuse the SSE proxy" = reuse the **polling loop**, not the wire format |
| D11 | Coexistence dispatch via an **explicit registry of React-owned path prefixes** (not a blind catch-all), deterministic precedence over Jinja blueprints | 18 Jinja blueprints exist; `/investigations` exists in both worlds | Same URL must deterministically resolve to React once migrated |
| D12 | RBAC + SocketIO are in-scope concerns for Phase 2 retirement (preserve/port/drop) | `permissions.py` + `websocket/` exist today | Deleting the Jinja path would orphan authz and collaboration features |
| D13 | **Jinja retirement scope narrowed** (`[H]` 2026-08-06): Task 6.3's AC becomes "no **migrated** route is served by Jinja," with the 10 un-migrated nav routes kept as a **documented Jinja remainder** | Task 6.1 found all six admin-gated blueprints have no React destination in this plan, making the original "no route served by Jinja" unsatisfiable | Honest scope over aspirational scope; full retirement becomes a later increment with parity targets already pinned in [route-parity-targets.md](../design/route-parity-targets.md) §7 |
| D14 | **Cutover URL strategy: 302-redirect bare Jinja URLs → `/app/*`** (`[H]` 2026-08-06, resolves Q9) | Bundle is hard-wired to `/app` (Vite `base` + Router `basename`); serving it at a bare path renders nothing | Simplest and lowest-risk; bookmarks keep working via redirect; every existing e2e/permalink/dev-proxy assumption about `/app` stays valid; `/app` becomes the permanent visible prefix |

## Open Questions

| # | Question | Impact | Status |
|---|----------|--------|--------|
| Q1 | (R3) Permission model for the migrated UI — preserve the two-tier RBAC (`require_role`) in React/BFF? | Security surface at Jinja retirement | **RESOLVED (Task 6.1, `[H]` approved 2026-08-06)** — **Port + harden identity.** Port the `user`/`admin` split to React/BFF AND replace the spoofable identity source (unverified JWT `groups` claim; `X-Beeper-Role` header honored in production) with verified auth; gate the header to non-production. See [ADR 0001](../specs/decisions/0001-rbac-and-realtime-collaboration-in-react-ui.md) §0(a). **Expands Task 6.2** — split the hardening out if too large, but do not revert to port-as-is. |
| Q2 | (R5) Is best-effort `component` derivation enough, or add a dedicated CRD/investigator field? | Triage-glance "component" quality | Open — evaluate after Task 2.3. **2026-08-11 scoping review: deliberately not resolved.** All four backlog candidates were scoped and none addresses Q2; inventing a CRD `component` field alongside the two `InvestigationStatus` additions already queued (RFC 0003's queue marks, RFC 0005's correlation edges) would add a third uncoordinated schema change to a **hand-written, pruning** CRD template. **Re-evaluate after Task 9.3** (RFC 0004's structured `signals[]`): that is the first time the UI receives per-step `{layer, source, query, value}` rather than LLM prose, which is the most plausible non-CRD source for a trustworthy component label. If derivation from `signals[]` is still insufficient, *then* the dedicated field is justified — and it should ride along with a CRD change already in flight, not open a fourth. |
| Q3 | Exact `/design-sync` input format for the chosen library | Library build/export shape (NFR23) | **Resolved (Task 4.4, 2026-07-30)** — full sync ran: converter consumes `dist-lib/index.js` + `dist-lib/index.d.ts` (via a `types` field in package.json) + the Storybook build as fidelity oracle. All 15 components uploaded to Claude Design project 616ba082 ("Beeper Design System"); sync state in `.design-sync/`. |
| Q4 | React app location (`ui/frontend/`) + Docker/static wiring | Build/deploy; blocks all later tasks | **RESOLVED (Task 1.1)** — `ui/frontend/` approved by the user 2026-06-23; `ui/Dockerfile` `node-builder` stage + `react_shell_bp`. (Row was left stale; closed retroactively 2026-08-11.) |
| Q5 | NFR21 "SSE render ≤2 s" vs the BFF's 3 s operator-poll floor | Detail-stream latency target | **Resolved (Task 1.6)** — lowered JSON stream poll to 1 s via `_DEFAULT_JSON_SSE_POLL_INTERVAL = 1` in `investigations.py`; NFR21 is met. HTML SSE stays at 3 s. Interval is configurable via `JSON_SSE_POLL_INTERVAL` Flask config key. |
| Q6 | Is the SocketIO collaboration surface (annotations/approvals/redirections) in or out of the React migration? | Phase 2 scope | **RESOLVED (Task 6.1, `[H]` approved 2026-08-06)** — **OUT: drop the surface, preserve the data.** Retire `ui/beeper_ui/websocket/` + the Jinja panel; `/socket.io/*` → `410 Gone` + RFC7807 body. The Qdrant `collaboration_messages` collection is **explicitly NOT deleted** — history stays recoverable. See [ADR 0001](../specs/decisions/0001-rbac-and-realtime-collaboration-in-react-ui.md) §0(b). |
| Q7 | Correct `docs/specs/architecture.md` baseline (no native operator SSE; RBAC + WebSocket exist) | Doc accuracy for downstream agents | Open — doc fix outside this plan. **Three further corrections found in the 2026-08-11 scoping review, all verified against code, and all worth folding into the same doc fix:** (1) the **operator API has no authentication of any kind** — `operator/src/api.rs` builds a bare `axum::Router` with no auth layer, exposed as a ClusterIP Service, already serving unauthenticated *state mutations* (`POST /api/v1/investigations/:id/{confirm,reject,resolve,verify}`) and the notification outbox that egresses to Slack/PagerDuty/email; the ADR 0002 two-tier model is a **BFF-local** property, not a system-wide one (addressed as NFR31 / main.md Task 7.0d); (2) `require_role("user")` is a **pass-through**, not a check (`permissions.py` branches only on `role == "admin"`) — it means "the resolver did not 401 you," which matters in `local`/`oidc` but is not a content gate (addressed as NFR30 / Task 7.0b); (3) the operator has **no kube-runtime reflector or Store anywhere**, so any `Api::list()` is a live API-server call, not a cached read — relevant to any doc claiming watch-cache behavior. |
| Q9 | **The `REACT_OWNED_PREFIXES` cutover is not wired end-to-end for non-`/app` URLs.** `serve_react_shell` serves `dist/index.html` verbatim at whatever bare Jinja path matched (e.g. `/knowledge`), but the bundle is built with Vite `base: '/app/'` and React Router `basename: '/app'` — so at a bare path the router matches no route. Untested today because `REACT_OWNED_PREFIXES` defaults to `()`. | **Blocks the actual Jinja→React cutover (Task 6.3), not the 5.1–5.4 builds** (those target `/app/*`). | **RESOLVED (`[H]` approved 2026-08-06)** — **redirect bare → `/app` (D14).** At cutover, a React-owned bare Jinja URL 302-redirects to its `/app/*` equivalent instead of serving the shell in place. Old bookmarks keep working via redirect; the `/app` prefix becomes the permanent visible URL; the bundle's `base`/`basename` stay untouched (all existing e2e/permalink tests remain valid). Task 6.3 implements; found in Task 5.0. See [route-parity-targets.md](../design/route-parity-targets.md) §6.5. |
| Q10 | Three core design tokens failed WCAG AA contrast (4.5:1). | Affected every view. | **RESOLVED (Task 6.0, `[H]` token values approved by user 2026-08-08).** Tokens lightened in-hue: `primary` #6366f1→**#8284f4**, `text-muted` #64748b→**#8391a6**, `status-critical` #ef4444→**#f37373**, `status-muted` #6b7280→**#989ea9**; new `--color-on-primary` #0f0f1a for text on a solid primary fill. axe `color-contrast` rule **re-enabled** and green. |
| Q11 | **Where does verified user identity come from?** Task 6.2's approved "harden" half assumes an identity to verify, but the UI has **no authentication at all** — no login, no IdP, no ServiceAccount, no JWT library, and React sends no auth header. Verifying a K8s SA token would only authenticate *the pod*, not a person. | **Blocks Task 6.2b.** Until answered, production has no admin path (6.2a fails closed by design, per the approved "don't ship the spoofable identity source" directive). | **RESOLVED (2026-08-08)** — user directive, verbatim: *"Beeper should support OIDC/SSO in front with SCIM for group passthrough and assignment. But if SSO is disabled then there should still be an admin interface that allows admins to group users into Admins or Users groups."* Maps onto the existing two-tier model: IdP "Admins"/"Users" groups → `admin`/`user` roles. Architecture recorded in [ADR 0002](../specs/decisions/0002-oidc-scim-and-local-fallback-identity.md) (**Status: Proposed**) — **the `[H]` approval of ADR 0002 (Task 8.1) is the gate for all implementation.** Task 6.2b is superseded by Milestone 2.3 below. |
| Q8 | Task 1.6's `GET /api/v1/investigations/{id}` returns **no per-step evidence values or embedded KB entries** — that data lives only in the Jinja-only `evidence_service`/`kb_service` paths | 2.5 renders the spec's "absent" states correctly today, but **real** Prometheus/Loki evidence + KB entries (FR25/FR26) need a backend extension to the 1.6 detail endpoint before 2.6/3.x show live data | **RESOLVED (design, 2026-08-11) → [RFC 0004](../specs/rfcs/0004-investigation-evidence-exposure.md) + Milestone 2.4 below.** Not an extension to the 1.6 endpoint: a **separate side-channel** `GET /api/v1/investigations/{id}/evidence`, fetched after first paint and never inside the 1 s SSE loop, because evidence is a slow-changing/optional/expensive projection of a fast-changing/required/cheap resource — joining them would put a Qdrant scroll + N KB lookups on both the NFR19 first-paint path and Task 2.6b's every-reconnect backfill. `investigation_detail_json` stays byte-identical (golden-fixture guarded). Three findings the original row did not capture: (a) the evidence **does not exist** for a running investigation — the investigator persists once, in `_finalize`, and `Agent.run()`'s exception path never reaches it (85/104 failures persisted nothing); (b) what *is* persisted is a **formatted prose blob** (`raw_signal_detail`), not structured query/value pairs, so FR25's shape needs either regex-parsing it or a new `signals[]` field; (c) the surviving Jinja `investigation_related_kb` route **violates FR26** by running a fresh semantic search at display time and must **not** be preserved as parity. |

## Phase 1: React Foundation & Incident-Triage Surface (First Increment)

### Milestone 1.1: Foundation & Tooling

| # | Task | Complexity | Dependencies | Status |
|---|------|-----------|--------------|--------|
| 1.1 | Scaffold Vite + React + TS (`ui/frontend/`); add a **Node build stage** to `ui/Dockerfile` (multi-stage: build `dist/` → copy into runtime); Flask serves `index.html` + hashed assets | L | None | done |
| 1.2 | Tailwind config porting the dark-first tokens (palette, spacing, motion, typography) as the single source of truth; no-hardcoded-values + reduced-motion lint | M | 1.1 | done |
| 1.3 | BFF **route-dispatch registry** — explicit React-owned path prefixes served the shell with deterministic precedence; `/api/*`, SSE, and unmigrated Jinja routes untouched | M | 1.1 | done |
| 1.4 | Component-library + Storybook scaffold (shadcn-style); **define the library API / extraction boundary**; one skeleton story per planned component; trial `/design-sync` ingest | L | 1.2 | done |
| 1.5 | Local dev inner loop — Vite dev server + HMR with `server.proxy` forwarding `/api/*` and the event endpoint to `:5000` (SSE buffering off); two-terminal workflow documented | S | 1.1 | done |
| 1.6 | **BFF JSON API for React** — `GET /api/v1/investigations` (list; `status`/`service`/`severity` filters → `list_investigations`), `GET /api/v1/investigations/{id}` (detail metadata + ordered steps), and `/{id}/events` (JSON event stream reusing the operator-polling loop, emitting `{order, step, status}`); decide poll interval vs NFR21 (Q5) | L | 1.1 | done |
| 1.7 | Test harness — Vitest + RTL + Playwright in CI | S | 1.1 | done |
| 1.8 | Draft terminology glossary (investigation-list + detail labels) so views are built against standardized copy (FR52 "before each view") | S | None | done |

**Task 1.1 AC:** `[T]` `npm run build` emits a static `dist/`; CI typecheck+lint clean · `[T]` UI Docker image builds the bundle via the Node stage and serves `index.html` + hashed assets (smoke test) · `[H]` repo location/build wiring approved (Q4)
**Task 1.2 AC:** `[T]` Tailwind theme exposes the dark-first tokens; a sample component resolves them · `[T]` lint fails the build on hardcoded color/spacing/motion values (FR51) and on animated elements lacking a `motion-reduce:` path
**Task 1.3 AC:** `[T]` a React-owned prefix returns the shell; an unmigrated route still renders Jinja · `[T]` collision test: `/investigations` resolves to React once registered, Jinja before (deterministic precedence)
**Task 1.4 AC:** `[T]` `dist/` exports ≥1 named component importable by an external consumer (ESM/`require` check) — not just "Storybook builds" · `[H]` a trial `/design-sync` run ingests the bundle and documents the expected format (resolves Q3); formal NFR23 gate is Task 4.4
**Task 1.5 AC:** `[T]` `vite dev` proxies `/api/*` + the event endpoint to the BFF; an SSE/event response streams through the proxy without buffering
**Task 1.6 AC:** `[T]` list + detail endpoints return **JSON** (not HTML); detail includes metadata + ordered steps · `[T]` the events stream emits ordered JSON events + a terminal event on completion · `[T]` poll interval is configurable; Q5 decision recorded (interval ≤ target, or NFR21 amended)
**Task 1.7 AC:** `[T]` a sample Vitest test and a Playwright e2e run green in CI
**Task 1.8 AC:** `[H]` draft glossary committed to `docs/` (self-reviewed against ≥2 current screenshots) before Tasks 2.2/2.5 finalize copy

**Parallelizable:** 1.1 and 1.8 first (1.1 is the hard gate, Q4); then {1.2, 1.3, 1.5, 1.6, 1.7} concurrently; 1.4 after 1.2. Up to 8 concurrent.
**Milestone Value:** a building, tested, BFF-served React app — tokens wired, route dispatch defined, a JSON event stream, fast local dev, and a design-sync-ready library scaffold. The platform every later task builds **into**.

**Completed:**
- **1.1** (merged `a38690b`) — Vite 8 + React 19 + TS at `ui/frontend/`; `ui/Dockerfile` `node-builder` stage; `react_shell_bp` serves the SPA at `/app/*` (registered last, no Jinja shadowing). `[T]` build → `dist/` proven by `ui/frontend/scripts/verify-build.mjs` (8/8); `[T]` Flask serving proven by `ui/tests/test_react_shell.py` (5/5); 48/48 existing UI tests still green. `[H]` repo location `ui/frontend/` approved (Q4). Docker multi-stage inspected, not `docker build`-run in-worktree.
- **1.8** (merged `0f0730e`) — `docs/design/terminology-glossary.md`. `[H]` approved by user 2026-06-23: keep `Condition`; job-phase `Failed` → **"Analysis Failed"** (OD-1); OD-2/3/5 defaults applied (see §13).
- **1.2** (merged `28f395a`) — Tailwind v4 + `ui/frontend/src/theme/tokens.css` (single source of truth; surfaces/brand/status/text/step-border/spacing/motion/typography + breakpoints sm 768 / lg 1200). `[T]` tokens resolve → `src/test/token-resolution.test.tsx` (39); `[T]` lint fails on hardcoded/motion-reduce violations → `src/test/lint-enforcement.test.ts` (2) via `eslint-plugin-tailwindcss`. 41/41 green. **Note:** Vitest is now wired here, so Task 1.7 shrinks to Playwright + CI. `no-custom-classname` is `warn` until the scaffold `App.tsx` migrates.
- **1.6** (merged `03b42a8`) — JSON API in `ui/beeper_ui/routes/investigations.py`: `GET /api/v1/investigations` (list+filters), `/{id}` (metadata + ordered steps), `/{id}/events` (JSON SSE reusing the poll loop). `[T]` list/detail JSON + `[T]` events ordered+terminal + `[T]` configurable interval → `ui/tests/test_api_v1_investigations.py` (29). **Q5 resolved:** JSON stream polls at 1 s (`JSON_SSE_POLL_INTERVAL`), meets NFR21 ≤2 s; HTML SSE stays 3 s. 2362 passed, 0 regressions.
- **1.3** (merged) — `ui/beeper_ui/routes/react_registry.py`: config-driven `REACT_OWNED_PREFIXES` + `before_request` dispatcher that short-circuits to the SPA shell for registered prefixes (deterministic, order-independent); `/api/*` + `/app/*` hard-excluded; registry ships empty (real prefixes added in M1.2). `[T]` shell-vs-Jinja + `[T]` `/investigations` collision toggle → `ui/tests/test_react_route_registry.py` (15). Also fixed a latent Task-1.1 test bug: Vite base64url hashes contain `-`/`_`, so `test_react_shell.py`'s asset regex now allows them (5/5 green).
- **1.4** (merged `5d14688`, status `done*`) — `ui/frontend/src/lib/` shadcn-style library (Radix + Tailwind on the 1.2 tokens; barrel `src/lib/index.ts` = extraction boundary): `InvestigationCard`, `StatusBadge` (14-value union encoding job-phase "Analysis Failed" vs workflow "Failed"), `InvestigationStep`, `SummaryHeader`, `RelatedKbPanel` + Storybook stories. `build:lib` → `dist-lib/` ESM + `.d.ts` (via `vite.lib.config.ts`, separate from `vite.config.ts` so 1.5 won't collide). `[T]` dist exports importable named components → `src/lib/test/dist-import.test.ts` (46/46). **`[H]` trial `/design-sync` DEFERRED to Task 4.4** (user, 2026-06-23 — Option A: avoid the outward-facing publish mid-foundation); `DESIGN_SYNC.md` prepared. Minor: 3px step border → `border-l-2`, `max-h:50vh` → `max-h-96` (Tailwind-token-safe; M1.2 revisit).
- **1.5** — `ui/frontend/vite.config.ts` `server.proxy`: a scoped regex entry for `/api/v1/investigations/{id}/events` (explicit no-buffering: default `selfHandleResponse` keeps http-proxy's per-chunk pipe-through; `configure` re-asserts `Cache-Control: no-cache` / `X-Accel-Buffering: no` on both the outgoing request and proxied response) plus a general `/api` entry for list/detail JSON, both targeting `BFF_ORIGIN` (env-overridable, defaults to `http://localhost:5000`). Manually verified against a live `vite dev` + stub SSE server: chunks land at the client as they're written (~500 ms apart), not batched at connection close; confirmed the fix for an initial double-pipe bug (a stray manual `.pipe()` alongside http-proxy's automatic one). `[T]` resolved-config assertions (target/changeOrigin/no-`selfHandleResponse`/`configure` header side effects/path-matching) → `ui/frontend/src/test/dev-proxy.test.ts` (5/5); 51/51 frontend tests green. Two-terminal workflow (`poetry run flask run` + `npm run dev`, mirroring `make tailwind-watch` + `flask run`) documented in `ui/README.md`. Production build unaffected (`server.proxy` only applies to `vite dev`).
- **1.7** (merged) — Playwright added (`playwright.config.ts` + `e2e/app-shell.spec.ts`, webServer = `vite preview` on `:4173`); new **"Frontend UI"** CI job in `.github/workflows/ci.yml` (separate from "Python UI"): `npm ci` → typecheck → **`build:lib`** (needed so 1.4's `dist-import.test` passes) → vitest → `playwright install chromium` → build → e2e. `[T]` Vitest + Playwright green → `e2e/app-shell.spec.ts` (2). Verified on merged base in CI order: **51/51 vitest + 2/2 e2e** (fresh `npm ci`).

**Milestone 1.1 COMPLETE (8/8).** Foundation ready: scaffold + Docker/serve, dark-first tokens, route-dispatch registry, component library, BFF JSON API, dev-proxy, CI test harness, glossary. Next: **Milestone 1.2** (list + detail views).

*(1.4's deferred `[H]` trial `/design-sync` was satisfied by Task 4.4's full sync on 2026-07-30 — status lifted from `done*` to `done`.)*

### Milestone 1.2: Incident-Triage Surface (list + detail at parity)

> Components are authored **as library primitives from the start** (extract-as-you-go, D3/1.4), each with a Storybook story — not page-local JSX refactored later.

| # | Task | Complexity | Dependencies | Status |
|---|------|-----------|--------------|--------|
| 2.1 | Layout shell + sidebar (icon rail; expand ≥1200 push / collapse <1200 overlay; auto-collapse on detail route; 60fps + reduced-motion; **focus management** on route change) | L | 1.2, 1.4 | done |
| 2.2 | Investigation **list** — first-seconds row facts; active/high-severity-first; status-group filter; empty/waiting + **Pending** states; **skeleton** load; **scroll restoration**; error boundary | L | 1.4, 1.6, 2.1, 1.8 | done |
| 2.3 | "Affected component" derivation from `condition`/anomaly signal (best-effort) | M | 2.2 | done |
| 2.4 | Problem-state plain-language heuristic mapping + raw-`condition` fallback | M | 2.2 | done |
| 2.5 | Investigation **detail** — immediate summary header; step progress + inline evidence; first-evidence emphasis; Related KB panel; Failed + Pending + KB-"0 entries" + FR48 placeholder; skeleton; "not found" error boundary | L | 1.4, 1.6, 2.1, 1.8 | done |
| 2.6a | SSE/event consume — 4-state lifecycle, "Reconnecting…" indicator, **auto-scroll rule** (only if within 100px of bottom) | M | 2.5, 1.6 | done |
| 2.6b | Reconnect **backfill** — ordered/idempotent re-fetch via `GET /api/v1/investigations/{id}`; "Live updates unavailable" fallback | M | 2.6a | done |

**Task 2.1 AC:** `[T]` collapsed = 64px icon rail (tooltips); ≥1200 expand **pushes** content, <1200 expand **overlays** (no width shift); detail route auto-collapses regardless of viewport (FR41/42/44) · `[T]` on detail entry incl. **cold permalink load**, focus moves to summary `<h1>`; on back, to the active nav item (WCAG 2.4.3) · `[T]` sidebar transitions honor `prefers-reduced-motion` (0ms under reduce)
**Task 2.2 AC:** `[T]` each row shows service/component/problem-state/severity/age, no horizontal scroll (FR45/46) · `[T]` active+high-severity first; status-group filter correct; **Pending** rows visually distinct from Running · `[T]` empty group → waiting state, never blank (FR22) · `[T]` cold load shows a list **skeleton**, never a blank frame (NFR19) · `[T]` back from detail restores list scroll position
**Task 2.3 AC:** `[T]` component derived from representative demo `condition` strings; unknown input degrades gracefully
**Task 2.4 AC:** `[T]` plain language for payment/cart/cpu signals (`http_*_5xx*` → "HTTP 5xx error rate elevated (N%)") · `[T]` no-match → raw anomaly description (FR47)
**Task 2.5 AC:** `[T]` summary header (service/severity/signal-count/status) renders from metadata **immediately, before any event arrives** (NFR19) · `[T]` steps render ordered with inline Prom values + Loki excerpts (FR25); first evidence step gets the emphasis treatment (reduced-motion instant) · `[T]` Related KB panel renders as an anchored bar showing "N Related KB Entries", expands upward ≥1200px / stacks inline <1200px (FR26) · `[T]` Failed → completed steps + distinct failure notice, no conclusion (FR23); Pending → "waiting to start" placeholder; absent/unparseable KB → "0 Related KB Entries" without error; absent correlation → "impact: not yet correlated" (FR48 placeholder) · `[T]` invalid id → "Investigation not found" in the shell (sidebar visible), not a generic 404 · `[T]` cold load shows a detail skeleton
**Task 2.6a AC:** `[T]` simulated disconnect shows "Reconnecting…" and transitions the 4 states (FR27) · `[T]` new events auto-scroll only when the user is within 100px of the bottom; otherwise append without scrolling · `[T]` first paint interactive ≤3s; event renders ≤ the Q5-decided latency (NFR21)
**Task 2.6b AC:** `[T]` on reconnect, missed steps backfill in order via `GET /api/v1/investigations/{id}` (idempotent, deduped) · `[T]` after repeated failures the "Live updates unavailable — refresh to sync" fallback shows; detail stays viewable

**Parallelizable:** after 2.1, run {2.2, 2.5} concurrently; 2.3+2.4 follow 2.2; 2.6a→2.6b follow 2.5 (one chain — 2.5 owns the `investigations/{id}` REST client). 2.1's 60fps check is the only `[H]`-ish gate — start 2.1 first. Up to 8 concurrent.
**Milestone Value:** the incident-triage hero (list + detail) runs in React at full parity — SSE, evidence, focus, skeletons, all lifecycle states.

**Completed:**
- **2.1** — `react-router-dom` + `@radix-ui/react-tooltip` added; router root (`src/App.tsx`, `basename: '/app'`) with route placeholders (`src/routes/{AppLayout,InvestigationListPage,InvestigationDetailPage}.tsx` — Task 2.2/2.5 build the real views). Library primitives (barrel-exported, Storybook stories) in `ui/frontend/src/lib/`: `Sidebar` (Observe/Learn/Manage groups, icon-rail collapsed / full expanded, Radix `Tooltip` per group in collapsed mode, router-agnostic via injectable `linkComponent`) and `AppShell` (skip link, top bar with hamburger + breadcrumb slot, content-area margin driven by push/overlay state) at `src/lib/components/{Sidebar,AppShell}/`. State machine `useSidebarState` (`src/lib/hooks/useSidebarState.ts`) models the spec's 3-value `auto | collapsed | expanded` route mode + a manual override (in-memory only, cleared on route change, still overridable per FR44's "always" clause even on the detail route) and viewport tracking via `matchMedia`. Focus management via `useRouteFocusManagement` (`src/lib/hooks/useRouteFocusManagement.ts`): detail-route mount (cold load or in-app nav alike) focuses `#detail-summary-heading`; returning from detail focuses `[data-sidebar-nav-active="true"]`. Reduced motion follows the established `transition-sidebar` + `motion-reduce:transition-none` token pattern (no JS matchMedia branching). `[T]` icon rail/push/overlay/auto-collapse → `src/lib/test/{useSidebarState,Sidebar,AppShell}.test.{ts,tsx}` (32) + `e2e/sidebar-behavior.spec.ts` (6, real-browser viewport resize) · `[T]` focus management (in-app nav) → `src/lib/test/useRouteFocusManagement.test.tsx` (4); (cold load) → `e2e/focus-management.spec.ts` (3) · `[T]` reduced-motion → `src/lib/test/sidebar-reduced-motion.test.tsx` (4) + the reduced-motion e2e case. `e2e/app-shell.spec.ts` updated for the new shell (Task 1.7's scaffold assertions retired). CI order verified clean from fresh `npm ci`: typecheck → `build:lib` → 91/91 vitest → `build` → 12/12 Playwright. Follow-up for Task 2.2/2.5: mount inside `<AppLayout>`'s `<Outlet/>`; detail route's real `SummaryHeader` `<h1>` must carry `id="detail-summary-heading"`; list route's active sidebar item needs `data-sidebar-nav-active` preserved (already on by `Sidebar`) for the back-navigation focus target to resolve.
- **2.5** (merged `f745f60`) — `src/routes/InvestigationDetailPage.tsx` + `src/api/investigation-detail.ts` (owns the detail REST client; 2.6 reuses). New lib primitives: `StepEvidence`, `FailureNotice`, `NotFoundMessage`, `DetailSkeleton`, `useIsNarrowViewport`; `SummaryHeader` gained `headingId` (sets `id="detail-summary-heading"` on the real `<h1>` → 2.1 focus mgmt verified). Immediate header (NFR19), ordered steps + first-evidence emphasis, Related KB anchored-bar ≥1200 / inline <1200, and all states (Failed/Pending/KB-0/FR48-placeholder/not-found-in-shell/skeleton). `[T]` → `src/routes/test/InvestigationDetailPage.test.tsx` + mappers/hook/client tests. Glossary honored (OD-1 `failed`→`analysis-failed`, OD-5 `investigating` from workflow state).
- **2.2** (merged, barrel-conflict resolved additively) — `src/routes/InvestigationListPage.tsx` + `src/api/investigations-list.ts`. Pure `status-group.ts` (active/resolved/failed + active-high-severity-first ordering) and `row-view-model.ts` (Pending-vs-Running via workflow_state; `condition` passthrough is the 2.3/2.4 seam) — both URL-migration-ready for 3.1. New lib primitives: `InvestigationListSkeleton`, `StatusGroupFilter`, `EmptyGroupState`; `InvestigationCard` gained a `component` subtitle slot + `linkComponent`. `useScrollRestoration` (window-level, `sessionStorage`, ready-gated) + a one-line `preventScroll:true` fix to 2.1's focus hook. `[T]` → list/api/scroll/row tests + `e2e/investigation-list.spec.ts`. **Combined-merge fix:** the scroll-restore e2e now uses browser Back (the detail route auto-collapses the sidebar per FR44, so no per-item link) — passes 3/3. Combined base re-verified in CI order: **202 vitest + 18 e2e**.
- **2.3** (merged `3179585`) — `src/lib/investigations/derive-component.ts`: heuristic that extracts the affected component from the `condition` string (endpoint path like `/checkout`, container/pod refs, metric family), wired into `InvestigationCard`'s `component` slot; unknown input degrades gracefully. `[T]` → `src/lib/test/derive-component.test.ts`. (Runtime conditions from `metrics.rs`/`logs.rs` carry metric names but not endpoints — reinforces Q2 on a dedicated field.)
- **2.6a** (merged `c7f9eb5`) — live SSE consume on the detail view: `useInvestigationEvents` (EventSource, injectable factory) drives a 4-state lifecycle (connected/disconnected/reconnected/failed) + `SseConnectionIndicator` "Reconnecting…"; `useAutoScrollOnAppend` auto-scrolls only within 100px of the timeline bottom; `mergeLiveStepEvents` merges live `{order,step,status}` into the ordered steps. `[T]` → `useInvestigationEvents.test.ts` (24), `useAutoScrollOnAppend.test.ts` (5), mapper tests. Backfill/"Live updates unavailable" is 2.6b (clean seam left, reuses `investigation-detail.ts`). **Note:** both 2.3 and 2.6a agents hit the background-stream watchdog (600s stall) at the finish line — 2.3 had already committed; 2.6a was completed by the orchestrator (finished tests + added no-op **EventSource** and **scrollIntoView** jsdom stubs to `src/test/setup.ts`, matching the ResizeObserver pattern). Combined base: **243 vitest + 18 e2e**.
- **2.4** (merged `9b66ece`+`728197b`) — `src/lib/investigations/problem-state.ts`: `deriveProblemState(condition)`, ordered `{pattern→template}` rules (HTTP 5xx / error-rate / latency-p99 / memory / cpu / queue-depth / SLO-burn), grounded in the real demo + `metrics.rs`/`logs.rs`/`burn_rate.rs` condition shapes; no-match → raw condition, empty → non-blank placeholder (FR47). Wired into row (`InvestigationCard.problemState` slot, emphasized above `component`) + detail `SummaryHeader`. `[T]` → `problem-state.test.ts` (22) + row/list integration.
- **2.6b** (merged `dac7cc6`+`f95c376`) — completes the SSE chain: `InvestigationDetailPage` passes `onReconnect` into `useInvestigationEvents`; on `disconnected→reconnected` it re-fetches via `fetchInvestigationDetail` and merges through new `mergeBackfilledSteps` (dedupe-by-`order`, idempotent, state-rank tiebreak so a stale snapshot can't undo live progress), layered under `mergeLiveStepEvents`. `failed` state → "Live updates unavailable — refresh to sync" + Refresh button; header/steps/KB stay viewable. `[T]` → mapper (9) + page reconnect/fallback tests.

**Milestone 1.2 COMPLETE (7/7).** The incident-triage surface runs in React: shell+sidebar, list (facts/filter/skeleton/scroll), detail (summary/steps/evidence/KB/all states), component + problem-state derivation, and live SSE with reconnect backfill + failure fallback. Combined base: **284 vitest + 18 e2e**. Next: **Milestone 1.3 (permalinks)**. (2.4+2.6b both touched `InvestigationDetailPage.tsx` — import conflict resolved additively.)
- **2.3** — `src/lib/investigations/derive-component.ts`: pure regex-based heuristic parsing the exact `condition` shapes the detectors emit (`operator/src/detection/metrics.rs` `"Metric {name} spike|drop: ..."`, `operator/src/detection/logs.rs` `"Error rate spike: ..."`, `operator/src/slo/burn_rate.rs` `"SLO burn rate alert: ..."`), mapping metric names to a family label (HTTP/RPC, CPU, Memory, Database, Message queue, Container) with a humanized-raw-name fallback for unrecognized metrics, covering the repo's demo fault scenarios (`make demo-fault FAULT=payment-failure/cart-failure/kafka-problems/high-cpu`). Unparseable/null/empty condition → `null` (component line omitted, no crash). Wired into `row-view-model.ts`'s new `component` field (replacing the Task 2.2 `condition`-passthrough placeholder in `InvestigationListPage.tsx`); `condition` itself is left untouched for Task 2.4. `[T]` → `src/lib/test/derive-component.test.ts` (20 cases) + 3 wiring cases added to `row-view-model.test.ts`; `InvestigationListPage.test.tsx`'s row-facts case updated to assert the derived label instead of the old raw-condition passthrough. CI order verified clean: `npm ci` → typecheck → `build:lib` → **223 vitest** → `build` → **18 e2e**. No `package.json` changes.

### Milestone 1.3: Permalinks (cross-cutting on the triage surface)

| # | Task | Complexity | Dependencies | Status |
|---|------|-----------|--------------|--------|
| 3.1 | URL-encode **list** state — status-group filter (+ future filters) in query params → `list_investigations(status=…)` | M | 2.2 | done |
| 3.2 | URL-encode **detail** state — investigation id (path) + anchored step (`#step-<id>`); cold load auto-collapses sidebar (FR44) + focuses header | M | 2.5 | done |
| 3.3 | Content-vs-chrome boundary — assert sidebar/scroll are **local**, not encoded; reload hydrates from URL params only | S | 3.1, 3.2 | done |
| 3.4 | Permalink-integrity e2e suite — cold-load deep links reproduce the same view | M | 3.1, 3.2 | done |

**Task 3.1 AC:** `[T]` selecting a filter updates the URL; cold-loading it reproduces the filtered list (FR53) · `[T]` maps to the existing `status` query, no operator change (R6/D6)
**Task 3.2 AC:** `[T]` a detail URL with `#step-<id>` cold-loads anchored to that step · `[T]` cold load auto-collapses the sidebar (FR44) and focuses the summary header
**Task 3.3 AC:** `[T]` **negative test** — encoding a filtered-list URL with the sidebar manually expanded <1200px, then cold-loading it, does **not** restore the expanded sidebar or prior scroll offset (resets to viewport default) · `[T]` cold load hydrates entirely from URL params via the operator API
**Task 3.4 AC:** `[T]` Playwright cold-loads a filtered-list URL and a **Completed**-investigation detail URL (anchored to a step) and asserts identical rendered view; includes the scroll-restoration assertion (NFR24)

- **3.2 (done)** — every rendered `InvestigationStep` now carries a stable `id="step-<order>"` (`order`, not `key`, since `key` can be `null`). New `useStepAnchorScroll` hook (`src/lib/hooks/`, exported via the lib barrel) reads `useLocation().hash` in `InvestigationDetailPage` and, once the target step exists in the DOM, `scrollIntoView({ block: 'center' })`s it — instant under `prefers-reduced-motion`. Re-runs on every `steps.length` change (not just mount) so it still resolves once the async one-shot fetch's steps render after a cold load, and remembers the handled hash so a later live-SSE step append doesn't re-yank scroll position. **Reconciliation with Task 2.1's header focus:** the anchor hook only ever calls `scrollIntoView`, never `.focus()`, so it never competes with `useRouteFocusManagement` focusing `#detail-summary-heading` (which itself passes `preventScroll: true`) — the two are non-competing by construction, proven together in one e2e test rather than assumed. (Incidental finding, not fixed — out of this task's scope: Task 2.6a's `useAutoScrollOnAppend` also fires a `scrollIntoView` on the timeline's bottom sentinel the instant steps first arrive; `useStepAnchorScroll` is wired to run after it in `InvestigationDetailPage.tsx` so the anchor's call is always the final one and wins the resting scroll position.) `[T]` → `useStepAnchorScroll.test.ts` (8), `InvestigationDetailPage.test.tsx` step-anchor describe block (4), `e2e/detail-permalink.spec.ts` (5, incl. the combined sidebar-collapse+header-focus+anchor-scroll cold-load proof). CI order verified clean: `npm ci` → typecheck → `build:lib` → **296 vitest** → `build` → **23 e2e**. No `package.json` changes.
- **3.1 (done, merged `d4cfa21`)** — list status-group filter is now a shareable permalink: `?status=active|resolved|failed` on `/app/investigations`, read/written via `useSearchParams` in `InvestigationListPage` (replaced local `useState`), with an `isStatusGroup` guard in `status-group.ts` (kept router-agnostic per 2.2) falling back to `active` on absent/invalid. **Deliberate R6/D6 nuance:** the fetch stays **unfiltered** (client-side group filtering preserves the cross-group count badges); a new tested `deriveListFetchStatus` always resolves to `undefined`, explicitly guarding the `failed`-vocabulary collision (group `failed` == API raw status `failed`) that a naive `{status: group}` passthrough would silently narrow on. `[T]` → `InvestigationListPage.test.tsx` URL-permalink + fetch-args describe blocks, `status-group.test.ts` guard tests, `e2e/investigation-list.spec.ts` cold-load test. Combined base (3.1+3.2) re-verified: **307 vitest + 24 e2e**.
- **3.3 (done, merged `01d2385`)** — negative test only (no source change; `useSidebarState` keeps manual override in memory / cleared on route change, `useScrollRestoration` uses `sessionStorage` — chrome never touches the URL). `e2e/permalink-chrome-boundary.spec.ts`: at <1200px, manual sidebar expand + scroll do NOT survive a **`browser.newContext()`** cold-load of the same `?status=` URL (sidebar resets to collapsed, scrollY=0), while the filter reproduces from the URL; and a fresh-context `page.goto` hydrates the filtered list purely from URL+API. (`newContext` proves URL-only hydration vs. leftover `sessionStorage`.)
- **3.4 (done, merged `4f9ffa3`)** — the NFR24 integrity gate, no source change (3.1/3.2 already correct). `e2e/permalink-integrity.spec.ts`: cold-load a filtered-list permalink → identical filtered/ordered list + restored scroll; cold-load a **Completed** investigation detail `#step-<order>` → identical detail + anchored step scrolled into view (closes the 3.2 follow-up: the completed-status × step-anchor combo).

**Milestone 1.4 progress:**
- **4.2 (done, merged `6ab7dff`)** — library finalized for design-sync: all 15 components have complete stories (gaps closed: `InvestigationStep` deploy type; `InvestigationCard` component/problem-state slots), enforced by a new **story-coverage test** (`src/lib/test/story-coverage.test.ts`, 32) so no component graduates without a story; `dist-import.test.ts` widened to the full barrel (15 components + 7 hooks); token lint clean (FR51); `DESIGN_SYNC.md` refreshed to the real inventory for 4.4. Q3's three open items (execution vs static analysis, d.ts bundling, dist-lib package.json) remain for the actual 4.4 run.
- **4.1 (done, merged; `[H]` APPROVED by user 2026-07-13)** — full FR52 glossary + §15 density audit (`docs/design/terminology-glossary.md`): §9 step labels reconciled to shipped code ("Metric Query"/"Log Query"/"KB Query", `correlation` documented), §14 lint coverage (16 rules + 4 justified exclusions) enforced via `npm run lint:terms` + `src/test/legacy-label-lint.test.ts` (`[T]`). Density resolutions (user 2026-07-13): **D1 FIXED** — impact line renders only with real `correlated_services` (supersedes the 2.5 FR48-placeholder AC); **D2 FIXED** — no type badge on `summary` steps (per approved 1.8 decision) → `InvestigationStep.test.tsx`; **L1 DEFERRED** (severity chip re-emphasis; revisit after 4.4). SummaryHeader severity-color bug spawned as a separate follow-up chip. Combined base after fixes: **345 vitest + 28 e2e**, `lint:terms` clean.
- **4.3 (done; `[H]` PASSED 2026-07-19)** — triage-glance acceptance: 2 non-builder reviewers, shown the 3 demo incidents cold (`demo_ui.py` :5050 serving the real built React UI), each named service / problem state / severity **≤5 s on every incident** — formal pass recorded in [triage-glance-test.md](../design/triage-glance-test.md) (NFR19). Setup found + fixed a real integration bug en route: Vite `base` had to be `/app/'` for Flask-served assets (`9d421f7`; vite-preview e2e had masked it). Harness: launch config `beeper-demo`; protocol sheet committed for future re-runs.

**Milestone 1.3 COMPLETE (4/4) — permalinks shipped.** Any triage view is a shareable link: list filter in `?status=`, detail step in `#step-<id>`, cold-load reproduces the identical view, chrome stays local — all proven by e2e. Combined base: **307 vitest + 28 e2e**. **Phase 1 Milestones 1.1–1.3 done; only Milestone 1.4 remains** (design-sync trial, language-pass, first-increment triage-glance acceptance — the `[H]` gates).

**Parallelizable:** 3.1 + 3.2 concurrent; 3.3 then 3.4. Up to 8 concurrent.
**Milestone Value:** any triage view is a shareable link — copy the URL, a colleague opens the identical view. The session's headline requirement, proven by test.

### Milestone 1.4: Design-sync, language pass & first-increment acceptance

| # | Task | Complexity | Dependencies | Status |
|---|------|-----------|--------------|--------|
| 4.1 | Full terminology glossary + per-view visual-density audit (extends the 1.8 draft) | M | 1.8, 2.2, 2.5 | done |
| 4.2 | Finalize/polish the component library (extracted as-you-go in 1.2); enforce token-only styling; complete Storybook stories | M | 2.1, 2.2, 2.5 | done |
| 4.3 | Triage-glance acceptance test with 2 non-builder reviewers | S | 2.2, 2.4 | done |
| 4.4 | Full `/design-sync` pass on the finalized library | M | 4.2 | done |

**Task 4.1 AC:** `[H]` glossary + density audit completed and self-reviewed against ≥2 screenshots, committed; a second pass runs with the 4.3 reviewers (FR52) · `[T]` migrated views contain no stray legacy labels (lint against the glossary)
**Task 4.2 AC:** `[T]` library builds to the `dist` bundle; no-hardcoded-values lint passes (FR51); every first-increment component has a Storybook story
**Task 4.3 AC:** `[H]` two reviewers who did not build it, shown a running investigation cold, each name **service + problem state + severity** within 5s across ≥3 recorded incidents. **Component is NOT in the 5-s bar this increment** (D7/Q2); naming it is a bonus (NFR19)
**Task 4.4 AC:** `[H]` `/design-sync` ingests the finalized library without error and a Claude Design pass renders at least `InvestigationCard` (active/completed/failed), `StatusBadge` (all variants), `InvestigationStep`, `SummaryHeader`, and `RelatedKbPanel` correctly (NFR23, FR51)

**Parallelizable:** 4.1 starts when 2.2 lands; 4.2 when 2.1/2.2/2.5 land; 4.3 after 2.4; 4.4 after 4.2. Both `[H]`-bearing (4.1, 4.4) overlap with Milestone 1.3 work. Up to 8 concurrent.
**Milestone Value:** the first increment is accepted — tightened language, a design-sync-ready library, a witnessed triage-glance pass.
**Observational Outcomes:** `[O]` reduced time-to-triage and fewer "where do I find X?" moments, via a post-rollout SRE survey vs. the Jinja baseline.

## Phase 2: Route Migration & Jinja Retirement (Later Increment)

Migrate remaining inventory routes view-by-view at parity (NFR20), each permalinked and a11y-parity (NFR22), then retire Jinja — **including** the RBAC and SocketIO decisions (D12).

### Milestone 2.1: Remaining route migration

| # | Task | Complexity | Dependencies | Status |
|---|------|-----------|--------------|--------|
| 5.0 | Confirm the live route inventory; pin each route's parity target (FR or named Jinja template) | S | Phase 1 | done |
| 5.0b | **Shared nav scaffolding** — route-derived `activeItemId`/breadcrumb; canonical hrefs; one placeholder page file per 5.1–5.4 route so the four tasks never touch `App.tsx`/`AppLayout.tsx` concurrently | S | 5.0 | done |
| 5.1 | KB browse/search + entry detail (search query URL-encoded — permalink) | L | 5.0, 5.0b | done |
| 5.2 | Ingestion/Detection Stats view (auto-refresh, FR32) | M | 5.0, 5.0b | done |
| 5.3 | Sources + LLM Spending views | M | 5.0, 5.0b | done |
| 5.4 | Metrics view — parity target pinned by 5.0 | M | 5.0, 5.0b | done |
| 5.5 | Accessibility-parity audit across all migrated views (axe, keyboard, focus, reduced-motion) | M | 5.1–5.4 | done |

**Task 5.0 AC:** `[T]` each migrated route has a written parity target (FR id or `templates/<name>.html`) before its task starts — no "parity with what?" gaps
**Task 5.1 AC:** `[T]` KB browse/search at parity; search query URL-encoded + cold-loads (FR53/FR29) · `[T]` entry detail at parity
**Task 5.2 AC:** `[T]` detection/ingestion fields render and auto-refresh without manual reload (FR32)
**Task 5.3 AC:** `[T]` Sources status + Spending render at parity (FR34/FR35)
**Task 5.4 AC:** `[T]` Metrics view renders the parity target pinned in 5.0
**Task 5.5 AC:** `[T]` axe passes on every migrated view; keyboard nav + visible focus verified (NFR22)

**Parallelizable:** 5.0 → 5.0b first (both are shared gates); then 5.1–5.4 concurrent; 5.5 after. Up to 8 concurrent.
**Milestone Value:** every navigation route runs in React at parity.

**Completed:**
- **5.0 (done, merged `29db926`)** — `docs/design/route-parity-targets.md` pins a written parity target for all 16 sidebar nav destinations: per-route Jinja URL(s), canonical React URL, the future `REACT_OWNED_PREFIXES` value, FR id(s) and/or exact `templates/<name>.html`, dual-mode/HTMX handling, permalink state, and data source. Tasks 5.1–5.4 get a full block each (§2–§5); the other 10 nav routes are recorded as "later increment, target pinned" (§7) so no future task re-derives the inventory. **Task 5.4's open target resolved:** `/metrics/` is the **MTTR Trends Dashboard**; no FR in `docs/reqs/main.md` covers MTTR reporting, so `templates/metrics/mttr.html` is the sole target — deliberately no invented FR id. `[T]` each migrated route has a written parity target → `ui/tests/test_route_parity_targets.py` (9): `TestNavCoverage::{test_doc_file_exists,test_sixteen_nav_items_parsed_from_layout,test_every_sidebar_nav_destination_has_a_doc_entry}`, `TestParityTargetsResolve::{test_six_per_task_parity_target_bullets_present,test_each_task_block_parity_target_resolves,test_section7_table_has_ten_later_increment_rows,test_section7_table_parity_targets_resolve}`, `TestMetricsTask54ParityTarget::{test_metrics_parity_target_is_the_mttr_template,test_metrics_parity_target_does_not_invent_an_fr_id}`. It is a real guard, not a tautology: it parses the doc against `layout.html`'s live nav declaration and `docs/reqs/main.md`'s FR list, and **was verified to fail** when a cited template path or FR id is fabricated (independently re-confirmed by the orchestrator). Full UI suite 2382 passed; ruff clean.
- **5.0b (done, merged `17927c5`)** — orchestrator-inserted shared scaffolding. Task 5.0 escalated that `activeItemId="investigations"` and the breadcrumb were **hardcoded** in `AppLayout.tsx`, and that all four of 5.1–5.4 would otherwise have to edit `App.tsx` + `AppLayout.tsx` — a guaranteed four-way merge conflict. Landed first so the four migrations are conflict-free: route-derived active-nav matching (longest-href-prefix, so `/knowledge/:entryId` still highlights Knowledge Base and nothing highlights on the catch-all); generalized breadcrumb (**investigation-detail copy unchanged** — regression-guarded); Knowledge Base id/href corrected `knowledge-base` → `knowledge` per the parity doc; and **one placeholder page file per downstream route** (`KnowledgeBasePage`, `KnowledgeEntryPage`, `IngestionStatsPage`, `SourcesPage`, `SpendingPage`, `MetricsPage`) all registered in the router now. Tests → `ui/frontend/src/routes/test/AppLayout.test.tsx` (25: active-item across all 7 routes + catch-all, breadcrumb incl. the detail regression guard, placeholder-renders-in-shell). CI order verified: **370 vitest + 28 e2e**, typecheck/lint/`lint:terms` clean.
  - **Contract for 5.1–5.4 (binding):** `App.tsx` and `AppLayout.tsx` are **final** — no downstream task edits either. 5.1 rewrites `KnowledgeBasePage.tsx` + `KnowledgeEntryPage.tsx`; 5.2 rewrites `IngestionStatsPage.tsx`; 5.3 rewrites `SourcesPage.tsx` + `SpendingPage.tsx`; 5.4 rewrites `MetricsPage.tsx`. Each may add its own `src/api/*`, `src/lib/*` (+ story, per the story-coverage test), tests, and BFF JSON endpoints. Shared files (`src/lib/index.ts` barrel, `src/test/setup.ts`, `ui/beeper_ui/routes/__init__.py`) must be edited **additively only**.
  - **Each of 5.1–5.4 must add its own BFF JSON API** — the Jinja KB/health/sources/spending/metrics routes return HTML, so there is no JSON data source for React yet. Model on Task 1.6's `investigations_api_bp` (`ui/beeper_ui/routes/investigations.py`) and reuse the existing service layer rather than reimplementing it.
- **5.1–5.4 — resumed 2026-08-06** (user approved parallel fan-out after the 2026-08-04 spend-limit halt; that halt lost no work — worktrees were empty and were removed to keep the plan truthful).
- **5.2 (done, merged 2026-08-06)** — `/app/ingestion-stats` renders the FR32/FR33 dashboard: metric tiles (metrics/logs received, anomalies detected/suppressed, active detectors), EWMA warmup progress bar (`samples/minimum*100`, clamped), and the three-state chip with precedence red "No Data" → amber "Warming Up" → green "Active" (reused `StatusBadge` variants, no parallel component). Auto-refresh = 5 s client-side poll via a new generic `useAutoRefresh` hook (pauses on `document.hidden`); a refresh never re-shows the skeleton. New BFF endpoint `GET /api/v1/ingestion/stats` (`ingestion_api_bp`, thin passthrough of `HealthService.get_ingestion_stats()`; Jinja route byte-identical). New lib primitives `MetricTile`/`EwmaProgressBar`/`IngestionStatsSkeleton` + stories. `[T]` fields render + auto-refresh without manual reload → `ui/frontend/src/routes/test/IngestionStatsPage.test.tsx` describe `IngestionStatsPage — auto-refresh without manual reload (the [T] AC)` (4 tests) + `e2e/ingestion-stats.spec.ts` real-wall-clock refresh test; chip precedence → `src/lib/test/pipeline-view.test.ts` + page-level `data-variant` asserts + e2e. Backend → `ui/tests/test_api_v1_ingestion.py` (14). Error semantics deliberately mirror Jinja 1:1 (failed refresh clears data and shows the error block).
- **5.3 (done, merged `bd5771c`)** — `/app/sources` (FR34) + `/app/spending` (FR35). Sources: status table with `StatusBadge` extended by a documented `connected|disconnected|unknown` axis (mirroring the Jinja `source_status_badge` exactly), last-seen, inline errors, 5 s refetch; `SourceService.get_sources()` dedup preserved by reuse. Spending: provider-config card with **server-side** key masking (the route never touches the raw env var; `test_never_returns_unmasked_api_key` exercises the real service through the endpoint), 4 metric cards, cap warnings, dependency-free SVG trend chart (`src/lib/spending/chart.ts`, a pure TS port of `_compute_spend_chart_data`), 30 s refetch. New endpoints `GET /api/v1/sources/` + `GET /api/v1/spending/` (503 + error body on operator failure). `[T]` Sources half → `ui/tests/test_api_v1_sources_spending.py::TestSourcesListEndpoint` (8) + `src/test/SourcesPage.test.tsx` (13) + e2e (3); Spending half → `::TestSpendingJsonEndpoint` (6, incl. the masking test) + `src/test/SpendingPage.test.tsx` (14) + e2e (3).
- **5.4 (done, merged `44cee92`)** — `/app/metrics` renders the MTTR Trends Dashboard at template parity (`metrics/mttr.html` + `_mttr_content` + `_drilldown`; deliberately no FR id per the 5.0 target): 3 summary cards, dependency-free inline-SVG `TrendChart` lib primitive, by-service/by-severity bars, expandable drilldown (inline section with local state — not a path route, `App.tsx` is frozen; deliberately not URL-encoded per the parity doc), export = `<a>` to the existing `/metrics/export` CSV with current filters. **`period`/`service`/`severity` are FR53 permalinks** via `useSearchParams` (cold-load reproduces the filtered view). New endpoints `GET /api/v1/metrics/mttr` + `/mttr/drilldown` reusing `MetricsService`; `formatMttr` is a 1:1 port of the Python formatter. `[T]` parity → `src/routes/test/MetricsPage.test.tsx` (19, covering every template element + the 4 filter-permalink tests) + `e2e/metrics.spec.ts` (6, incl. cold-load permalink) + `ui/tests/test_api_v1_metrics.py` (19). UX note: changing a filter closes a stale drilldown (fixes a latent Jinja stale-panel quirk, commented in code). Known dev-only gap: the export link 404s under `vite dev` (proxy covers `/api` only); correct under production serving.
- **5.1 (done, merged `ba17409`)** — `/app/knowledge` (browse + search) and `/app/knowledge/:entryId` (detail). Search is an FR53 permalink: typing debounce-updates `?q=`, and cold-loading a seeded URL reproduces the identical result (proven at unit + e2e level). New endpoints `GET /api/v1/knowledge/?q=&entry_type=&service=&date_range=` and `GET /api/v1/knowledge/<entry_id>` (`knowledge_api_bp`, reusing `KBService`/`EmbeddingService`; detail returns sanitized `content_html` + FR31 structured fields + related/source/contributing investigations). New lib primitives `KbEntryCard`/`KbListSkeleton`/`KbEntryNotFound` + stories. `[T]` browse/search+permalink → `ui/tests/test_api_v1_knowledge.py::TestKnowledgeListEndpoint` + `src/routes/test/KnowledgeBasePage.test.tsx` (debounce-URL + cold-load tests) + `e2e/knowledge-base.spec.ts`; `[T]` entry detail → `::TestKnowledgeEntryDetailEndpoint` + `KnowledgeEntryPage.test.tsx` + e2e cold-load/not-found tests. Deliberate scope notes: server-side `entry_type`/`service`/`date_range` filters have no UI yet (parity doc allows); KB write actions (edit/history/corrections) stay at their Jinja URLs (FR50 coexistence); search-term `<mark>` highlighting skipped. Sanctioned contract deviation: `AppLayout.test.tsx` placeholder assertions for the two knowledge routes swapped to dummy stand-ins (knowledge-scoped only, verified).
- **5.5 (done, merged `9ef27ad`)** — accessibility-parity audit; **8 real bugs found and fixed**, several serious: (1) WCAG 2.4.3 focus management was never wired for KB entry detail (`AppLayout`'s `isDetailRoute` was investigation-only) — fixed via a separate `isFocusManagedDetailRoute` flag so sidebar-collapse stays investigation-scoped, and the KB heading now renders unconditionally so the one-shot focus effect can't race the fetch; (2) the skip link never moved focus (`#main-content` lacked `tabIndex={-1}`, WCAG 2.4.1) — fixed in `AppShell`; (3) `TrendChart` had `role="button"` circles nested inside `role="img"` (axe `nested-interactive`, serious) — now `role="group"` when interactive, plus a visible SVG focus outline (Tailwind v4 gotcha: unconditional `outline-none` permanently zeroes `--tw-outline-style`, silently cancelling `focus-visible:outline`); (4) Ingestion Stats' whole-dashboard `aria-live` narrowed to the status chip (was re-announcing all counters every 5 s); (5) Sources table `<caption>`; (6) skeleton→data announcements via narrow text-gated `role="status"` regions on Sources/Spending; (7) Spending SVG chart replaced generic `aria-label` with an sr-only per-day data table (SVG `aria-hidden`); (8) KB markdown headings demoted via `normalizeContentHeadings` (no h1 collisions/`heading-order` violations). Focus rings aligned to the `focus-visible:ring-2 ring-primary ring-offset-2` convention. `[T]` axe half → `e2e/a11y.spec.ts` (21 tests, WCAG 2.0/2.1 A+AA, loaded/error/empty states, incl. investigations regression); keyboard/focus half → `e2e/keyboard-navigation.spec.ts` (11) + `e2e/focus-management.spec.ts` (+2 for the KB 2.4.3 fix). `App.tsx` untouched; `AppLayout.tsx` +14 commented lines (sanctioned by audit findings). **Escalation → Q10/Task 6.0:** systemic pre-existing token contrast defect; `color-contrast` rule disabled in the axe spec with inline justification until 6.0 lands.
- **Milestone 2.1 COMPLETE (7/7: 5.0, 5.0b, 5.1–5.5).** Every in-scope nav destination (KB, Ingestion Stats, Sources, Spending, Metrics) runs in React at parity with JSON APIs, permalinks, and audited accessibility. Final combined main: **556 vitest, 87 e2e, 2458 pytest, 0 TS errors**, ruff + lint:terms clean. Next: **Milestone 2.2** (6.0 contrast tokens → 6.2 RBAC port+harden per ADR 0001 → 6.3 narrowed Jinja retirement per D13/D14).
- **Post-merge integration (orchestrator-verified on combined main):** four-way merge (5.3 ff → 5.2 auto-merge → 5.4 and 5.1 each with the expected end-of-barrel conflict in `src/lib/index.ts`, resolved keeping all blocks). Combined: **515 vitest, 46 e2e, 2438 pytest, 0 TS errors** (after 5.1: **556 vitest, 53 e2e, 2458 pytest**) — all task counts additive with zero cross-task regressions; the 5.0b file contract held (no frozen-file edits, `routes/__init__.py` auto-merged). Gotcha for future sessions: `dist-import.test.ts` fails against a **stale `dist-lib/`** — always `npm run build:lib` before vitest when the barrel changed (it's the documented CI order).

### Milestone 2.2: Auth, collaboration & Jinja retirement

| # | Task | Complexity | Dependencies | Status |
|---|------|-----------|--------------|--------|
| 6.0 | **Fix the WCAG AA color-contrast token defect (Q10)** — Design System Agent review; redefine `primary`/`text-muted`/`status-critical` (or restrict usage), sweep affected components, re-enable the axe `color-contrast` rule | M | — (blocks no task, but do before new 2.2 UI) | done |
| 6.1 | Inventory `require_role()` gates + the SocketIO collaboration surface; decide preserve/port/drop for React (R3/Q1, Q6) | M | — (start in Phase 1) | done |
| 6.2a | **Port RBAC + fail-closed production gate** — gates on the six ungated `/api/v1/*` blueprints, `X-Beeper-Role` refused under production config, `/socket.io/*` → 410 (data preserved), fix the crafted-JWT 500 | M | 6.1, 5.5 | done |
| 6.2b | **Verified identity** — superseded 2026-08-08: expanded into **Milestone 2.3** (Tasks 8.1–8.9) per the Q11 resolution / [ADR 0002](../specs/decisions/0002-oidc-scim-and-local-fallback-identity.md). Its AC intent (verified identity, credential transmission, manifest tests) carries into 8.3–8.9 | L | 6.2a, Q11 | superseded |
| 6.3 | Retire Jinja for **migrated** routes (D13): bare URL → 302 to `/app/*` (D14), templates + render path removed; document the retained Jinja remainder | M | 6.2a | done |

**Task 6.0 AC:** `[H]` new token values approved by the user (visual identity change) · `[T]` all three tokens meet 4.5:1 against their paired surfaces (automated contrast test) · `[T]` the Task 5.5 axe suite passes with `color-contrast` re-enabled
**Task 6.1 AC:** `[H]` decision recorded for (a) the two-tier RBAC and (b) the SocketIO collaboration features (annotations/approvals/redirections) — preserved, ported, or explicitly dropped with rationale
**Task 6.2a AC** *(the achievable half of the approved "port + harden"; see the split note below)*: `[T]` role-gated routes enforce the decided policy in React/BFF (admin/user test) · `[T]` a conscious posture is chosen and tested for the **six ungated `/api/v1/*` blueprints** (investigations, knowledge, ingestion, sources, spending, metrics) — none carry a gate today · `[T]` **`X-Beeper-Role` is refused under production config** and still honored in development/testing (update `test_permissions.py::test_header_sets_admin_role`, do not delete it; note `ProductionConfig.__init__` is dead code — use a class attribute) · `[T]` a crafted JWT whose payload is valid JSON but not an object no longer 500s every route (`permissions.py` `claims.get` raises an uncaught `AttributeError` today) · `[T]` `/socket.io/*` returns `410 Gone` + `application/problem+json`, not a 500 · `[T]` **the Qdrant `collaboration_messages` collection is NOT deleted** — the approved decision retires the code surface only
**Task 6.2b AC** *(blocked on Q11)*: `[T]` **identity is verified** — a token that fails verification resolves to `user` (or is rejected), never `admin` · `[T]` the K8s identity chain exists (UI ServiceAccount + the RBAC needed by the chosen mechanism) and is covered by manifest tests · `[T]` the React client transmits credentials on `/api/v1/*` calls · `[H]` the auth-source decision (Q11) recorded before implementation
**Task 6.3 AC** *(narrowed 2026-08-06 per D13, `[H]` approved; original "no route served by Jinja" was unsatisfiable — see the Task 6.1 plan-gap finding)*: `[T]` no **migrated** route is served by Jinja — every prefix in `REACT_OWNED_PREFIXES` 302-redirects its bare URL to the `/app/*` equivalent (D14/Q9), and its Jinja templates + render path are removed · `[T]` the retained Jinja remainder (the 10 §7 routes in [route-parity-targets.md](../design/route-parity-targets.md)) is explicitly documented and still renders — no orphaned imports/includes from deleted templates · `[T]` redirects are covered by route tests (bare URL → 302 → `/app/*` → correct React view)

**In progress:**
- **6.1 (done, `[H]` APPROVED by user 2026-08-06, merged `8d60602`).** Produced `docs/specs/decisions/0001-rbac-and-realtime-collaboration-in-react-ui.md` (**Status: Proposed**). Investigation/doc only; no code touched.
  - **RBAC inventory:** 27 `require_role` call sites across 7 files — 20 `user`-tier, 7 `admin`-tier (all admin gates protect mutations: trust-level writes, gate-threshold writes, adjustment apply/reject, adaptive evaluate). **Critical identity finding:** `resolve_user_role()` (`ui/beeper_ui/middleware/permissions.py`) resolves role either from a K8s JWT `groups` claim that is **decoded but never signature-verified**, or from a raw `X-Beeper-Role` header honored verbatim **in every environment including production**. This is intentional and test-pinned (`test_permissions.py::test_header_sets_admin_role`), not a bug — but it means the two-tier RBAC is a **UI-affordance guard-rail, not a security control**. Recommendation: **Port** as-is (lowest regression, reuses 31 existing tests as spec) with that caveat explicit.
  - **SocketIO inventory:** 9 events; 6 persist to a durable Qdrant `collaboration_messages` collection. But `annotate`/`redirect`/`approve_fix`/`reject_fix` POST to operator endpoints (`/annotate`, `/redirect`, `/approve`, `/reject-fix`) that **do not exist** in `operator/src/api.rs` — failures are swallowed and the UI broadcasts success anyway, so "Approve" already does nothing. The panel is reachable only from the legacy Jinja detail route, and the React detail view has never referenced it. Recommendation: **Drop**, with `/socket.io/*` → `410 Gone` + RFC7807 `application/problem+json` body (satisfying Task 6.2's "defined response, not a 500").
  - **Q6 answered** by the SocketIO inventory (pending approval); **Q1 answered** by the RBAC recommendation (pending approval).
- **NEW PLAN GAP found by 6.1 — Milestone 2.1 does not cover every Jinja route, but Task 6.3's AC assumes it does.** All six blueprints carrying `admin`-tier gates (`trust_config`, `trust_settings`, `confidence_gates`, `notifications`, `notification_config`, `reports`) have **no React destination anywhere in this plan** — Milestone 2.1 migrates only KB / ingestion-stats / sources / spending / metrics. Task 6.3's `[T]` "no route is served by Jinja" is therefore unsatisfiable as written. Resolve before 6.3: either extend Milestone 2.1 with the 10 "later increment" routes pinned in [route-parity-targets.md](../design/route-parity-targets.md) §7, or narrow 6.3's AC to the migrated set and keep a documented Jinja remainder.

**Task 6.2 split (2026-08-07, orchestrator).** Pre-work found the "harden" half is far larger than the M estimate and is **not merely large — it is under-specified**: `ui/pyproject.toml` has no JWT/crypto/kubernetes dependency (only `certifi` transitively); the UI Deployment sets **no `serviceAccountName`** and there is no `ui-serviceaccount.yaml`/ClusterRole anywhere in `helm/`, so a TokenReview call would 403 today; React sends no auth header and has no shared fetch wrapper to add one to; and `ProductionConfig.__init__` is **dead code** under `from_object(<class>)`, so any production gate must be a class attribute, not constructor logic. Most fundamentally, the UI has **no authentication whatsoever** — so "verify the identity" presupposes an identity source that does not exist (Q11). Per the plan's own pre-authorized fallback ("split the hardening into its own task rather than silently reverting to port-as-is"), 6.2 is split into **6.2a** (everything achievable now, fail-closed) and **6.2b** (verified identity, blocked on Q11). This is a split, **not** a revert — 6.2a still refuses the spoofable header in production.

**Completed (Milestone 2.2):**
- **6.0 (done, `[H]` approved 2026-08-08, merged `6834593`)** — closes Q10. Four tokens re-tuned (lightened, same hue) and verified against **all three** surface tones (base/raised/overlay) they're actually used on, not just one. Added `--color-on-primary` (#0f0f1a) to resolve a genuine mathematical conflict the audit surfaced: primary-as-text-on-dark needs to be light while primary-as-fill behind text needs a dark foreground — mutually exclusive, so solid-primary buttons now use a dedicated dark foreground. `StatusBadge`/`KbEntryCard` badge variants moved off translucent tints to solid `bg-surface-overlay` (translucent tints dropped below 4.5:1 on hover-state background swaps even at corrected values). `docs/specs/ux-design-specification.md` colour table brought back in sync (was stale while cited as authoritative). `[T]` tokens meet 4.5:1 → `ui/frontend/src/test/contrast.test.ts` + `src/test/wcag-contrast.ts` (derives WCAG relative-luminance ratios from live token values — not hardcoded numbers); `[T]` axe suite with `color-contrast` re-enabled → `e2e/a11y.spec.ts` 21/21 (orchestrator-verified independently).
- **6.2a (done, merged `16611a3`)** — the achievable half of the approved "port + harden", **failing closed**. `[T]` role gates → existing `test_permissions.py` (semantics preserved); `[T]` six previously-ungated `/api/v1/*` blueprints now `@require_role("user")` — decision + reasoning documented in the module docstring, proven structurally by `test_json_api_rbac.py::TestSixBlueprintsAreDeliberatelyGated` (via a new `.required_role` introspection marker) and behaviourally by `::TestSixBlueprintsRemainReachable`; `[T]` production header refusal → `test_permissions.py::TestXBeeperRoleHeaderRefusedInProduction` (6) + `test_header_still_honored_under_development_config`, implemented via a `Config.ALLOW_ROLE_HEADER` **class attribute** (`ProductionConfig.__init__` is dead code under `from_object(<class>)`); `[T]` **crafted-JWT 500 fixed** → `::test_crafted_jwt_with_non_object_payload_does_not_crash` (the exact `x.MTIz.y` input, which previously raised an uncaught `AttributeError` inside `before_request`, 500-ing *every* route); `[T]` `/socket.io/*` → 410 + RFC7807 → `test_socketio_retired.py` (12, exact ADR §6 body across all methods/sub-paths); `[T]` **Qdrant data preserved** → `test_collaboration_service.py::TestCollaborationDataPreserved` (3, source-level sweep proving no delete/drop path) — orchestrator independently confirmed the only `delete`/`drop` occurrences in that module are explanatory comments.
  - **Kept `collaboration_service.py`** (against the ADR's "costs nothing" baseline) for a stronger reason: it is still actively read by the Jinja "human interventions" history.
  - **Test delta explained (not silent loss):** 2458 → removed 81 (60 `test_websocket.py` + 21 orphaned `InvestigationService` operator-forwarding tests, whose endpoints never existed) + added 53 = **2430**.
  - **⚠️ Fail-closed consequence, documented in three places** (`config.py` comments, a new operator-facing subsection in `docs/deployment-guide.md`, and ADR §8): with the spoofable header refused in production and 6.2b unbuilt, **production currently has no admin path** — admin-gated routes are unreachable there. This is the intended reading of the approved "don't ship the spoofable identity source" directive, and the deployment is ClusterIP / not internet-facing.
- **Combined main after both merges (orchestrator-verified):** **576 vitest, 87 e2e, 2430 pytest, 0 TS errors**, ruff + `lint:terms` clean.
- **6.3 (done)** — the final Milestone 2.2 / Phase 2 task. `REACT_OWNED_PREFIXES` is now `("/investigations", "/knowledge", "/health/ingestion", "/sources", "/spending", "/metrics")` (was `()`); `react_registry.py`'s `before_request` hook 302-redirects a matched, non-excluded path to `/app/*` (D14) instead of serving the shell in place (the original Task 1.3 behavior — now dead code, replaced). 20 Jinja templates deleted (the full-page/list-content/detail-content templates for the six migrated destinations, plus partials solely used by them); the twelve corresponding Jinja view functions (`list_investigations`, `investigation_detail`, `kb_index`, `kb_search`, `kb_entry`, `ingestion_stats`, `list_sources`, `spending_dashboard`, `spending_status`, `metrics_dashboard`, `mttr_partial`, `mttr_drilldown`) are gutted to a one-line defensive redirect rather than deleted outright — every one of them is still `url_for()`-referenced by a retained template (e.g. every "Back to Knowledge Base" link), so deleting the route would have broken `url_for` with a `BuildError`; a real request never reaches the body because the `before_request` hook always wins first.
  - **Un-migrated sub-routes preserved via two new mechanisms in `react_registry.py`:** `_REDIRECT_EXCLUSION_PATTERNS` (regex carve-outs for `/spending/costs*`, `/metrics/export`, every Knowledge write/history/admin route including the dynamic-`<entry_id>` ones, and the Jinja-only investigation action routes + both HTML SSE streams) and `_REDIRECT_TARGET_OVERRIDES` (5 bare paths — `/health/ingestion`, `/knowledge/search`, `/metrics/mttr`, `/metrics/mttr/drilldown`, `/spending/status` — whose React destination isn't a 1:1 path rewrite). The list-level HTML SSE (`/investigations/stream`, `_generate_sse_events`) was deleted outright (its only template, `_list_content.html`, is gone and nothing ever called it); the per-investigation HTML SSE (`/investigations/<id>/stream`) and all ten `require_role`-mixed investigation action routes (verify/confirm/reject/resolve/feedback/related-kb/linked-kb/gate-status/urgency/remediation-progress) were deliberately **kept live, not migrated** — React never called them and their partial templates survive untouched.
  - **Hardening found during test migration:** a `..`-laden path (e.g. `/investigations/../../etc/passwd/confirm`, from a pre-existing path-traversal-rejection test) doesn't URL-normalize in Werkzeug's test client, so it matched the `/investigations` prefix without matching any exclusion regex's exact `id/action` shape — before a fix, that redirected to an equally malformed `/app/...` URL instead of the pre-6.3 clean `404`. Fixed in `_is_always_excluded()`: any path containing a literal `..` segment is now permanently excluded from the registry, restoring the old `404` and closing what was, at minimum, a confusing behavior regression.
  - **Retained Jinja remainder navigation:** the sidebar (`components/layout.html`) and command palette (`command-palette.js`) now link the six retired destinations straight at their `/app/*` URL rather than the bare (now-redirecting) Jinja URL, avoiding a pointless extra hop; the ten un-migrated §7 destinations (Health, SLO, Services, Topology, Analytics, Reports, Handoff, Cost Insights, Notifications, Trust) keep their bare Jinja URLs unchanged.
  - **`[T]` AC1** (no migrated route served by Jinja) → `ui/tests/test_jinja_retirement.py::TestAC1AllSixMigratedPrefixesRedirect` (13 parametrized cases across every migrated URL shape) + `test_react_route_registry.py::TestGuttedViewsRedirectRegardlessOfRegistry` (proves the six views redirect unconditionally, not just via the registry).
  - **`[T]` AC2** (retained remainder still renders, no orphaned includes) → `test_jinja_retirement.py::TestAC2UnmigratedSubRoutesSurvive` (carve-outs incl. dynamic-`<entry_id>` ones don't redirect) + `test_no_orphaned_template_includes_after_deletion` (parses every `{% include %}`/`{% from %}` in the retained template tree and asserts the target still exists on disk) + every existing, untouched test suite for the ten §7 routes still passing unmodified + `test_route_parity_targets.py` (route-parity-targets.md's own per-route guard, extended with a RETIRED-bullet convention that asserts the opposite of its original check — the cited template must NOT exist — for the six retired blocks).
  - **`[T]` AC3** (redirect chain + query-string preservation) → `test_jinja_retirement.py::TestAC3RedirectChainReachesCorrectReactView` (follows the redirect into the built bundle, asserts `<div id="root">`) + `TestAC3QueryStringPreservation` (7 tests incl. multi-value `&foo=1&foo=2`, the `/knowledge/search` and `/metrics/mttr` target-override cases, and the no-query-string no-trailing-`?` case).
  - **Test delta explained:** 2430 → net **-148** (**2282** final, 0 failed, 0 skipped). Breakdown: ~450 test functions removed across 24 files (`test_investigation_routes.py` alone: 132→64) — all either directly asserted on now-deleted template rendering (bare list/detail/index/entry/search/dashboard pages, HTMX partials) or were three tests found to be *vacuously* passing post-retirement (checked only `"x" not in html`, trivially satisfied by a redirect's near-empty body) and removed as false-confidence coverage; offset by ~70 new/expanded test functions — the new `test_jinja_retirement.py` (54 collected cases from 17 functions, heavy parametrization) plus additions to `test_react_route_registry.py` (+13) and `test_route_parity_targets.py` (+2). Coverage lost at the Python/Jinja layer for the six migrated views (data rendering, error states, empty states) already has an equivalent at the React layer (`ui/frontend/src/test/*`, `ui/frontend/e2e/*`) and/or the JSON API layer (`test_api_v1_*.py`) that was never touched by this task — verified file-by-file, not assumed.
  - **Frontend needed zero source changes** — `ui/frontend/` is untouched; **576 vitest, 87 e2e, 0 TS errors**, `lint:terms` clean, all unchanged from the Milestone 2.1 baseline.
  - `ruff check .` clean.

**Milestone 2.2 status (2026-08-08): complete except 6.2b.** 6.0/6.1/6.2a/6.3 are done and merged; **6.2b (verified identity) is the only remaining task, blocked on Q11** — an `[H]` decision about where verified user identity comes from (options recorded in the Q11 row; the question was posed to the user once and interrupted — re-raise when they're ready, do not guess). *(Update, later 2026-08-08: Q11 resolved in direction; 6.2b is superseded by Milestone 2.3 / ADR 0002 — see the Q11 row and Milestone 2.3 below.)* Final combined main after 6.3: **576 vitest, 87 e2e, 2282 pytest, 0 TS errors**, ruff + `lint:terms` clean (pytest 2430 → 2282: ~450 template-rendering test functions removed with the 20 deleted Jinja templates, offset by `test_jinja_retirement.py` and other additions — full breakdown in the 6.3 entry). With D13's approved narrowing, the workstream Definition of Done reads: every **migrated** route is React-served with Jinja retired behind 302 redirects, RBAC posture is ported fail-closed, and collaboration is resolved per ADR 0001. Remaining future work beyond this plan's committed scope: 6.2b (Q11), the 10 §7 later-increment route migrations (targets pinned), and Phase 3 (externally blocked on RFC 0001 Phase 3).

**Parallelizable:** **6.1 is a decision task — start it during Phase 1 (overlaps, off the critical path)**; 6.2 after 6.1+5.5; 6.3 last (sequential).
**Milestone Value:** the React overhaul is complete for its narrowed (D13) scope — Jinja retired for every **migrated** route, RBAC/collaboration resolved. Not literally "every route React-served": ten nav destinations remain a documented, deliberate Jinja remainder (route-parity-targets.md §7 — Health, SLO, Services, Topology, Analytics, Reports, Handoff, Cost Insights, Notifications, Trust), with parity targets already pinned for whoever picks up full retirement as a later increment.
**Observational Outcomes:** `[O]` no increase in client-side error rate vs. the Jinja baseline after full cutover.

### Milestone 2.3: Identity & Access (ADR 0002 — supersedes Task 6.2b)

> Consolidates the Q11-resolution design program (OIDC + SCIM + local-fallback pillar designs, two adversarial reviews) per [ADR 0002](../specs/decisions/0002-oidc-scim-and-local-fallback-identity.md) (**Proposed**) and the FR54–FR62 / NFR25–NFR26 block in [docs/reqs/main.md](../reqs/main.md). One mode knob (`BEEPER_AUTH_MODE=none|local|oidc`, default `none`), one shared Qdrant identity store, one resolver, one frontend auth seam. Every step lands default-off: production stays fail-closed and the demo (`mode=none`) stays zero-config throughout. **Program-level gate at every task:** full pre-existing pytest suite (2282) green unmodified under `AUTH_MODE=none` + demo-values Helm manifest snapshot unchanged.

| # | Task | Complexity | Dependencies | Status |
|---|------|-----------|--------------|--------|
| 8.1 | **ADR 0002 `[H]` approval gate** — user blesses the four decision points (ADR §12: local-accounts fallback + login-required-in-local; store-primary + default-`user`-unprovisioned; `ui.flaskEnv` production default with demo opt-in; no break-glass) and confirms the reconstructed Q11 directive; ADR flips Proposed→Accepted; FR54–FR62/NFR25–26 confirmed landed | S | Q11 direction (have) | done |
| 8.2 | **Fail-closed hotfix** (independent of 8.1 — ship even if the rest slips): delete `_extract_role_from_k8s_token` + its call site in **all** modes (crafted-JWT-never-admin regression test retained as a permanent guard); correct the false `ProductionConfig` docstring ("every admin route 403s in production" is untrue today — the ungated Bearer peek runs first); Helm `ui.flaskEnv` (default `production`) + `make demo-up`/`demo-deploy` explicitly set `development`; `get_config()` unknown-env fallback flips to `ProductionConfig` | S | — | done |
| 8.3 | **Shared identity store + unified resolver + session core** — `identity_store.py` (`beeper_users`/`beeper_groups` per ADR §5: merged schema, `user_name_lc` key, adopt-and-link with authoritative role recompute, uniqueness lock, 60 s TTL cache, zero-admin CRITICAL+health flag); `BEEPER_AUTH_MODE` config + boot refusals (incl. env `SECRET_KEY` required in `local`/`oidc`); `resolve_request_identity()` per ADR §7; 401/403 contract + one table-driven exemption matrix; Origin/Referer CSRF check; SSE keepalive re-auth | L | 8.1, 8.2 | done |
| 8.4 | **Frontend auth seam + e2e auth fixture** — `src/api/http.ts` `apiFetch` (mode-aware 401 redirect: `oidc`→`/auth/login?next=`, `local`→`/app/login?next=`, `none`→never; typed 403); migrate all 8 existing API clients (static-sweep test); `GET /api/v1/auth/me` + failure-tolerant `useCurrentUser` (unreachable ⇒ `auth_mode:none`); shared Playwright auth fixture so the existing 87 e2e stay green | M | 8.3 | done |
| 8.5 | **OIDC login** — Authlib dep; `auth_bp` (`/auth/login\|callback\|logout`); full callback validation (sig/iss/aud/exp/nonce/state, JWKS rotation, UserInfo groups fallback); `BEEPER_ADMIN_GROUPS`/`BEEPER_USER_GROUPS` mapping (no callback refusal when SCIM on); identity-only session + snapshot rules per ADR §5.2; safe-`next` handling | L | 8.3, 8.4 | done |
| 8.6 | **Local auth + bootstrap** — `argon2-cffi`; `POST /api/v1/auth/login\|logout` (registered only in `local` mode; uniform 401s + constant delay); bootstrap seed (idempotent never-overwrite) + `flask create-admin` CLI; `/app/login` page + `LoginForm` lib primitive + story; route-parity-targets **§9 net-new-route convention** + guard-test variant | L | 8.3, 8.4 | done |
| 8.7 | **Admin users view** — `admin_users_api_bp` (`/api/v1/admin/users`, all routes `.required_role == "admin"`): list/create/PATCH role-active/reset; last-admin 409; scim-owned 409; `/app/admin/users` React view + `UserTable`/`RoleSelect`/`UserFormDialog`/`ConfirmActionDialog`/`OriginBadge`/`UserMenu` primitives + stories; role-aware Manage nav (affordance only) | L | 8.6 | done |
| 8.8 | **SCIM surface** — `scim_bp` (`/scim/v2` Users+Groups+discovery per ADR §4, both PATCH dialects, vendor quirk tolerances, SCIM error schema); bearer auth (dual-token, fail-closed, unregistered when disabled); mutation audit log with token fingerprints; store-primary `lookup(email_lc, external_id)` wiring + lifecycle guarantees (NFR25) | L | 8.3 | done |
| 8.9 | **Deploy, docs & live validation** — one `ui.auth` Helm tree + single `existingSecret` (no plaintext token values); optional SCIM NetworkPolicy; `deployment-guide.md` sections (modes, SCIM token = admin-equivalent secret + rotation runbook, bootstrap/lockout recovery, removing the "production has no admin path" caveat); ADR 0001 §8 closing note; `[H]` live end-to-end runs | M | 8.5, 8.6, 8.7, 8.8 | done |

**Task 8.1 AC:** `[H]` the four ADR §12 decision points approved (or amended) and the reconstructed Q11 directive confirmed; ADR 0002 status flipped to Accepted with the approval recorded · `[T]` FR54–FR62/NFR25–26 present in `docs/reqs/main.md`; doc-guard suites still green
**Task 8.9 — `[T]` complete (2026-08-09); `[H]` live validation prepared, not yet run.** The deploy/docs/tests layer that makes Milestone 2.3 operable. One `ui.auth` values tree (`helm/beeper/values.yaml`): `mode` (none/local/oidc, default `none`), `existingSecret`, `externalScheme`, `sessionLifetimeHours`, `adminGroups`/`userGroups`, `oidc.*`, `scim.{enabled,strict,networkPolicy}`, `bootstrap.enabled` — wired into `ui-deployment.yaml` as 19 identity env vars, exactly matching `config.py`/`bootstrap.py`'s names (greeped both directions by a static test; **no mismatch found**, so `config.py` needed zero changes). Single `existingSecret` pattern (6 keys: `secretKey`/`clientSecret`/`scimToken`/`scimTokenSecondary`/`bootstrapUsername`/`bootstrapPassword`), every `secretKeyRef` `optional: true` by design — a Secret missing a key degrades to the application's own boot-time (`validate_boot_config()`) or SCIM request-time (403) error rather than a Kubernetes `CreateContainerConfigError`; no Secret resource is templated by this chart (mirrors the pre-existing `llm.apiKeySecret` convention). **Demo invariance (NFR26): the entire identity env block sits behind one `{{- if ne .Values.ui.auth.mode "none" }}` guard, and `values-dev.yaml` is untouched** — a full `helm template` diff of this branch against pre-8.9 `main` (`53c9a12`) for BOTH default values and `values-dev.yaml` is **byte-for-byte empty** (`diff` exit 0, zero lines), the strongest form of the "unchanged" claim. Optional SCIM `NetworkPolicy` (`ui.auth.scim.networkPolicy.*`, disabled by default): implemented honestly as a whole-UI-port peer allow-list (NetworkPolicy is L3/L4 — it cannot restrict by HTTP path, so `/scim/v2` cannot be isolated from the rest of the UI's traffic on the same port); the template `fail`s at render time if `enabled: true` with an empty `allowFrom`, guarding the well-known Kubernetes footgun where an empty ingress `from` list means "allow all," not "deny all." Three ready-to-use per-mode values overlays + an example Secret manifest added under `helm/beeper/examples/`. `deployment-guide.md` gains "Authentication & Identity" (values reference, mode comparison) and an expanded "Beeper UI authentication & identity (ADR 0002)" replacing the old Task 6.2a section — two admin paths documented, SCIM-token-is-admin-equivalent + the full dual-token rotation runbook, bootstrap/lockout recovery via `flask create-admin`, and IdP setup notes for Okta/Entra/Keycloak (**Entra: SCIM `externalId` → `objectId`, not `sub`**, plus the RP-initiated-logout-without-`id_token_hint` caveat, both from 8.5's report). ADR 0001 §8 gains a closing note (history preserved, marked superseded, points at ADR 0002) rather than being silently rewritten. New live-validation runbook (`docs/runbooks/identity-live-validation.md`, `triage-glance-test.md`-styled): three scenarios (local bootstrap→login→manage; `oidc` against a disposable Keycloak — current coordinates verified via web search, `quay.io/keycloak/keycloak:26.7.0`, plain Deployment+Service + `kcadm.sh` realm/client/group fixture, not the heavier Bitnami chart; SCIM push simulation via `curl` against the live `/scim/v2` surface, correcting the AC's "unassign → 401" shorthand to the technically accurate split — group-unassign demotes to `403` without a session clear, `active:false` is what clears the session and produces `401`) — each with setup/protocol/expected-observations/a blank results table/rollback, ready for the orchestrator+user `[H]` session with zero improvisation. `[T]` linkage: `demo/tests/test_ui_auth_helm.py` (29: env-wiring symmetry, demo-invariance structural guard, secret-pattern/optional-secretKeyRef checks, NetworkPolicy honesty + fail-fast, plus a `helm`-binary-guarded golden-render tier skipped when the binary is absent) · `ui/tests/test_deployment_guide_identity_sections.py` (13, light doc-guard mirroring `test_route_parity_targets.py`'s pattern — pins the presence of every (a)-(e) deliverable section). `helm lint`/`helm template` clean in all 3 modes + default + `values-dev.yaml` (5 combinations). Suites: **2854 pytest (+13) / 767 vitest (unchanged) / 99 e2e (unchanged)**, demo suite **104 (+29, 75→104)**; `ruff check .` clean (no Python behavior touched — `config.py` needed zero edits). Everything left for the `[H]` session: the three live-validation scenarios in the runbook above — needs a running kind demo cluster (`make demo-up`) for all three; scenario 2/3 additionally need the disposable Keycloak fixture the runbook stands up and tears down itself. No merge/push performed (orchestrator-owned).
**Task 8.9 — done (build half merged `bae8f5f` 2026-08-09; `[H]` live validation PASSED and approved by the user 2026-08-11).** Live run: **19/19 steps across three scenarios** on the kind demo cluster with freshly-built images — full results tables + amendments in [identity-live-validation.md](../runbooks/identity-live-validation.md) §"Completed run". Scenario 1 (local, 8/8): bootstrap→login→create/promote/demote with the inline last-admin refusal, byte-identical failure 401s, CLI recovery. Scenario 2 (OIDC vs real Keycloak 26.7.0, 5/5): cold-permalink round-trip back to the original URL, groups→role mapping, in-shell 403 for the group-less user. Scenario 3 (SCIM, 6/6): store-primacy proven live in BOTH directions (group push flipped a live session user→admin with no re-login; unassign demoted to 403-with-session-intact within 60 s; deactivation cleared to 401), fingerprint-only audit trail flagged admin-group changes distinctly. **Four real defects found and fixed during the run** (each committed same-day with regression tests): (1) `identity_store`/`collaboration_service` read only `QDRANT_URL` (unset by the chart) and silently dialed localhost in-cluster — the pattern inherited from the never-exercised collaboration service (`aac7633`); (2) `create-admin` docs used `--app beeper_ui` which cannot resolve the factory → `--app beeper_ui.app`; (3) the FR58 SCIM audit trail was **silently dropped in-cluster** (no handler + unconfigured root; caplog-based tests structurally could not catch it) → dedicated INFO handler + emission-config tests; (4) bootstrap races Qdrant readiness on cold starts — degrades per FR61, recovery documented, plus the `zero_active_admins:false`-also-means-store-unreachable health-flag caveat. Program gate re-proven live: mode `none` restored, anonymous access intact, demo stack healthy (the pre-existing `demo-deploy` flagd ConfigMap ownership conflict was repaired en route). Suite after live-run fixes: **2859 pytest / 767 vitest / 99 e2e**.

**MILESTONE 2.3 COMPLETE (10/10: 8.1–8.9 incl. the 6.2 split).** The user's directive — OIDC/SSO with SCIM group passthrough/assignment, plus an admin interface for grouping users into Admins/Users when SSO is disabled — is fully delivered, tested, and live-validated. Remaining open beyond committed scope: the 10 §7 later-increment route migrations (targets pinned) and Phase 3 (externally blocked on RFC 0001 Phase 3). Helm: `ui.auth` values tree (mode/oidc/scim/groups/session/bootstrap) wired to all 19 `BEEPER_*` env names (symmetry statically tested against `config.py` — zero mismatches found); single `existingSecret` pattern (clientSecret/scimToken(+secondary)/bootstrap creds/secretKey, each `secretKeyRef` conditional; **no plaintext secrets in values**); example values files + Secret template under `helm/beeper/examples/`; optional disabled-by-default SCIM NetworkPolicy with an **honesty note** (L3/L4 only — peer-restricts the whole UI port as defense-in-depth; cannot path-isolate `/scim/v2`; template `fail`s on enabled-with-empty-`allowFrom` since empty `from` = allow-all). **NFR26 proven and orchestrator-re-proven: `helm template` output byte-identical to pre-8.9 main under BOTH default and demo values** (`diff` exit 0). Docs: deployment guide gains the three-mode reference, SCIM-token-=-admin-secret + dual-token rotation runbook, bootstrap/lockout recovery (`kubectl exec … flask create-admin`, zero-admin alarm), Okta/Entra/Keycloak notes (incl. **Entra externalId→objectId**, no-`id_token_hint` logout caveat); **6.2a's "production has no admin path" caveat retired** (ADR 0001 §8 closing note → ADR 0002). Runbook `docs/runbooks/identity-live-validation.md`: three `[H]` scenarios (local-mode lifecycle on kind; OIDC vs disposable Keycloak `quay.io/keycloak/keycloak:26.7.0` with self-contained setup/teardown; SCIM curl simulation) — one AC shorthand corrected en route: SCIM group-unassign ⇒ demote-to-`user` (403, session intact), `active:false` ⇒ session cleared (401); runbook tests both. `[T]` linkage: `demo/tests/test_ui_auth_helm.py` (29: env symmetry, demo invariance, secret pattern, NetworkPolicy, golden render) + `ui/tests/test_deployment_guide_identity_sections.py` (13). Suites: **2854 pytest (+13) / 104 demo (+29) / 767 vitest / 99 e2e unchanged**; ruff + helm lint clean.
**Task 8.7 — done (2026-08-09, merged `1c11700`).** The admin users view — the directive's SSO-off fallback made real. Backend `admin_users_api_bp` (`/api/v1/admin/users`, every route `.required_role == "admin"`): list (username/display/role/**origin** local-vs-SCIM/active), create-local-user (**`409 local-user-creation-unavailable` in `oidc` mode** — a well-formed request failing on server state, consistent with the other state-conflict 409s), PATCH role/active with **`409 last-admin`** and **`409 scim-owned-user`**, admin password reset (8.6 hashing rules; created user proven to log in end-to-end through the real login route). **Security review found and fixed a real TOCTOU race**: separate unlocked `would_orphan_admins()` + later-locked write could let two concurrent demotions jointly zero out all admins — fixed with an atomic `update_user(guard_last_admin=)` path + `LastAdminError` (additive store change, flagged+tested), plus a malformed-JSON-body 500→422 fix. CSRF: covered by the shared gate (`/api/v1/admin/*` not exempt) — verified, not assumed, with a regression test. Frontend: `AdminUsersPage` (`/app/admin/users`) + five lib primitives (`UserTable`/`RoleSelect`/`UserFormDialog`/`ConfirmActionDialog`/`OriginBadge`, Radix dialog dep added) each with stories; role-gated Manage→Users nav (**UI affordance only — API enforces**, commented as such); in-shell 403 for non-admins, no redirect. Parity doc §9b filled. `[T]` linkage: `test_admin_users_api.py` (61: RBAC structural+behavioral, create/patch/reset, last-admin incl. **atomicity proof**, scim-owned both directions, oidc-create-refusal, malformed-body, CSRF, login-e2e) · `AdminUsersPage.test.tsx` (17) · component tests (5 files) · `e2e/admin-users.spec.ts` (5, incl. axe-clean + nav-hidden + in-shell 403). Suites: **2841 pytest (+63; incidental +18 from dist/ unblocking pre-existing skips) / 767 vitest (+94) / 99 e2e (+5)** — orchestrator-verified fresh post-merge, incl. the frontend-engineer's barrel/story-coverage regex fix (UserFormDialog restored to the primary per-component check; 28 components resolve, no phantoms). Delivery note: two sub-agents stalled mid-task; the Tech Lead detected the silent gaps, resumed/raced them safely, and completed the remainder itself.
**Task 8.6 — done (2026-08-09, merged `b7a6859` + merge-resolution fix).** *(Orchestrator merge notes: two cross-branch semantic conflicts resolved post-merge — `_OidcConfig` in `test_local_auth_routes.py` needed 8.5's `BEEPER_OIDC_*` boot-refusal stubs, and 8.6's local-mode `create_app()` boot side effect (bootstrap → store singleton) leaked into unrelated mode-`none` `/health/api` exact-body tests → global autouse `reset_identity_store()` teardown added in conftest. Combined main after 8.6: **2778 pytest / 673 vitest / 94 e2e**, all exact.)* Local username/password login + bootstrap (ADR 0002 §6, FR59/FR61). `password_hashing.py`: argon2id via `argon2-cffi`, RFC 9106 §4's "second recommended option" (m=65536 KiB/64 MiB, t=3, p=4 — the low-memory profile, matching this single-replica pod; not the 2 GiB/t=1 highmem profile), `MIN_PASSWORD_LENGTH=12`, and a module-level `DUMMY_PASSWORD_HASH` so an unknown-username login still runs a full Argon2id verify (CPU-time-normalized against a real user's wrong-password attempt). `routes/auth.py` gains two new blueprints alongside the existing `auth_api_bp`/`/me`: `local_auth_api_bp` (`POST /login`, registered **only** in `local` mode — 404 elsewhere) and `session_auth_api_bp` (`POST /logout`, registered in **both** `local` and `oidc` — see the oidc-logout decision below). Both self-enforce CSRF via `same_origin_request()` (this path is exempt from the shared `before_request` check). Login's uniform-401 mechanism: the failure branch always verifies against a REAL Argon2id hash (the record's own, or `DUMMY_PASSWORD_HASH` when no record/no local password exists) plus a `LOGIN_FAILURE_DELAY_SECONDS=0.3` wall-clock floor (`_apply_constant_delay`, injectable `_sleep`/`_monotonic` test seams) — applied only on failure, never on success (ADR's own wording ties the delay to "login failures"). Bootstrap (`services/bootstrap.py`, `ensure_bootstrap_admin()`): reads `BEEPER_BOOTSTRAP_ADMIN_USERNAME`/`_PASSWORD` straight from `os.environ` (never a frozen `Config` class attribute — same freshness reasoning as `SECRET_KEY`), idempotent (existing username never touched/overwritten), refuses a lone-set env var or a too-short password with a logged ERROR (no user created), and always finishes with the **one additive `identity_store.py` public method this task adds**, `refresh_zero_admin_alarm()` (flagged per the task's "minimal additive method" allowance — a read-triggered variant of the existing write-triggered zero-admin CRITICAL-log + `/health/api` flag, needed because bootstrap's no-write paths — seed absent, or already-seeded — must still raise the alarm). Wrapped in a broad `except Exception` so a Qdrant outage at boot degrades to a log line, never a crash. `flask create-admin` (`cli.py`, registered unconditionally in `create_app()`): `--password`/`--password-stdin`/interactive-`getpass`, promotes-and-reactivates an existing (e.g. accidentally-demoted) user rather than erroring, `kubectl exec`-friendly. Frontend: `LoginForm` (`src/lib/components/LoginForm/` + story, barrel-exported) is presentational+controlled (page owns the `apiFetch` call/loading/error state, matching `StatusGroupFilter`'s split); `LoginPage` (`src/routes/LoginPage.tsx`, registered in `App.tsx` as a **sibling** of the `AppLayout` route tree — no sidebar chrome) reads/validates `?next=` client-side (`login-next-path.ts`'s `resolveSafeNextPath`, `/app`-prefix-only) and navigates via `http.ts`'s newly-exported `navigateTo()` (the SAME seam `apiFetch`'s own 401→login redirect already uses, for one shared navigation convention + one shared test seam in both directions). `docs/design/route-parity-targets.md` gains **§9 "Net-new React routes (no Jinja ancestor)"** (`Jinja URL(s): none (net-new)` convention) documenting `/app/login` (9a) and reserving `/app/admin/users` (9b) for Task 8.7; `test_route_parity_targets.py` extended with `TestNetNewRoutesSection` (7 new tests) plus a `_milestone_2_1_body()` scoping fix so §9's own bullets don't perturb the pre-existing Milestone-2.1-specific counts. **`oidc`-mode logout decision:** `POST /api/v1/auth/logout` registers in BOTH `local` and `oidc` (not `local`-only) — it is a pure, mode-agnostic "clear whatever session cookie exists" action (the session module doesn't care whether `establish_session_identity()` was called by this task's `login()` or by 8.5's OIDC callback), giving the React UI ONE logout call site regardless of mode; it does not collide with 8.5's separate `POST /auth/logout` (no `/api/v1` prefix), which owns the heavier RP-initiated IdP-redirect flow. `[T]` linkage: `test_password_hashing.py` (12) · `test_local_auth_routes.py` (28, incl. an end-to-end login→`require_role("admin")`→200 test and its 403/401 mirror-images) · `test_bootstrap.py` (14) · `test_create_admin_cli.py` (6) · `test_route_parity_targets.py` (11→18) · `LoginForm.test.tsx` (10) · `login-next-path.test.ts` (13) · `LoginPage.test.tsx` (8) · `local-login.spec.ts` (5 e2e: redirect round-trip, uniform-error stay-on-page, unsafe-`next` fallback, 2× axe-clean) · suites **2578 pytest (+67) / 673 vitest (+34, incl. +1 in the pre-existing `e2e-auth-fixture-usage.test.ts` since it parametrizes over e2e spec files and a new one was added) / 94 e2e (+5)**, `ruff check .` clean, `oxlint`/`lint:tw`/`lint:terms` clean (pre-existing warnings untouched), full CI order (`typecheck`→`build:lib`→`test`→`build`→`e2e`) green. Visual spot-check via the browser preview (loaded + error states, dark tokens, hover/disabled states) — screenshots not persisted, described in the task report. Delivery note: `poetry.lock`/`node_modules` did not pre-exist in this worktree — both installed fresh (`poetry lock`+`install`, `npm ci`) before any test could run.
**Task 8.5 — done (2026-08-09, merged `8f530e3`).** OIDC login: `routes/oidc_auth.py` (`auth_bp`, registered only in `oidc` mode — disabled ⇒ 404 pattern): `GET /auth/login` (safe-`next` validation: `/app`-paths only), `GET /auth/callback` (full FR55 validation — JWKS signature **restricted to a hardcoded asymmetric-only allowlist via direct `joserfc.jwt.decode`**, deliberately bypassing Authlib's metadata-trusting wrapper which would admit HS256; exact iss/aud/exp/nonce/state; JWKS rotation retry; UserInfo groups fallback for Entra overflow), `POST /auth/logout` (self-enforced CSRF; local session always cleared; best-effort RP-initiated logout URL without `id_token_hint` since tokens are never retained). Groups→role per FR56 with the SCIM-on (no callback refusal, store-primary) vs SCIM-off (snapshot) split. Tokens validated then **discarded** — session-contents test proves no token material. `external_id` join key = `oid || sub` (Entra's `sub` is pairwise; **8.9 docs must tell Entra admins to map SCIM externalId → objectId**). **84 new tests** (`test_oidc_{config_boot_refusals,login,callback,logout}.py` + `_stub_oidc_idp.py` RSA-key stub IdP; `responses` dev-dep since Authlib rides `requests`, not httpx). Suite 2627 → **2711** on main. **Merge note:** three announced textual conflicts (routes registry, config, pyproject dev-deps) interleaved cleanly, plus one **semantic** cross-branch conflict the orchestrator resolved in the merge commit: 8.8's SCIM test configs (built before 8.5's boot refusal existed) needed the three `BEEPER_OIDC_*` stubs — the exact treatment 8.5 applied to the other pre-existing oidc-mode test files. Known scope limit: server-side login redirects for the 10 Jinja-remainder pages land on `/app` post-login (frontend only ever sends `/app/*` nexts).
**Task 8.8 — done (2026-08-09, merged, branch commit `002cb6e`).** SCIM 2.0 provisioning surface: `scim_bp` at `/scim/v2` (Users/Groups CRUD, `eq` filtering, PUT + **both vendor PATCH membership dialects**, deactivation, hard DELETE, discovery docs JSON-Schema-conformance-tested); constant-time dual-token bearer auth (`BEEPER_SCIM_TOKEN`/`_SECONDARY`, zero-downtime rotation; enabled-but-unconfigured ⇒ per-request **403** naming the misconfiguration per ADR §8 — not 401, since no credential retry can succeed; disabled ⇒ blueprint unregistered, plain 404); fingerprint-only audit log (`sha256[:8]`, logger `beeper_ui.scim.audit`, admin-group changes distinctly flagged). Lifecycle: deactivation→resolver-denial within TTL, adopt-and-link e2e incl. push-races-login, last-admin removal via SCIM **alarms without blocking** (admin-UI-only guard per design). **116 new tests** → suite 2511 → **2627** on main (orchestrator-verified post-merge). Flagged-and-accepted `identity_store` additions (all additive + tested: `get_by_external_id`/`delete_user`/`get_group_by_id`/`delete_group`/`update_group` + optional `update_user` kwargs + `GroupNotFoundError`) — the route layer genuinely needed resolve-by-SCIM-id and full-replace primitives. **Documented finding:** hard `DELETE /Users/{id}` makes the identity "never provisioned" → resolver default `user` (not 401) unless `BEEPER_SCIM_STRICT`; real IdP connectors deactivate first/instead, and ADR §5.3's delete⇒denial is fully realized via `active=false` — pinned by `test_hard_delete_degrades_a_live_session_to_default_user_not_strict`. Rotation runbook + Secret shape handed to 8.9. Delivery note: survived 3 API drops + 1 stall via wip-checkpoint commits.
**Task 8.4 — done (2026-08-09, merged `610d3d1`).** The frontend auth seam. `src/api/http.ts` `apiFetch`: same-origin credentials default, 401-with-`authentication-required`-type → mode-aware redirect (`oidc`→`/auth/login?next=`, `local`→`/app/login?next=`, `none` never; mode cached from one `/me` probe), 403 → typed `PermissionDeniedError` (never redirects). All 8 API clients migrated, each preserving its own error convention; `no-bare-fetch.test.ts` statically bans bare `fetch(` in `src/api/` forever. `GET /api/v1/auth/me` live (exempt prefix; `routes/auth.py`) + failure-tolerant `useCurrentUser` (unreachable ⇒ mode `none` — demo renders with zero auth infra). **SSE auth-failure UX:** `EventSource` hides HTTP status, so a terminal `failed` re-probes `/me` and redirects only if the session actually dropped — otherwise the existing "Live updates unavailable" banner stands (no false-positive redirect loops). Playwright auth fixture (`e2e/fixtures/auth.ts`) + a static guard forcing future auth-asserting specs through it. `[T]` linkage: `http.test.ts` (20) · `no-bare-fetch.test.ts` (18) · `test_auth_me.py` (17, all modes incl. store-wins-over-stale-snapshot and strict/non-strict unprovisioned) · `useCurrentUser.test.ts` (6) · suites **639 vitest (+63) / 89 e2e (+2) / 2511 pytest (+17)**, independently re-run by the orchestrator. One documented assertion swap in `test_identity_resolver.py` (exempt `/api/v1/auth` prefix: 404-no-blueprint → 200-anonymous). Noted for later sanity: `/me`'s shipped shape nests `user{}` vs ADR §6's flat prose example (documented in `routes/auth.py`); SCIM-strict unprovisioned reports `authenticated: true, role: null` (UX call for 8.5–8.7). Delivery note: survived 3 transient API drops + 2 watchdog stalls via wip-checkpoint commits; orchestrator ran final verification.
**Task 8.3 — done (2026-08-09, merged `2f09e47`).** The identity foundation: `identity_store.py` (`beeper_users`/`beeper_groups` Qdrant payload collections; adopt-and-link with authoritative role recompute; case-insensitive uniqueness; 60 s TTL cache; zero-active-admins CRITICAL+health flag; `would_orphan_admins()` last-admin primitive for 8.7), `middleware/session.py` (identity-only signed-cookie session + `same_origin_request()` CSRF + login-redirect helper), `resolve_request_identity()` unified resolver with the table-driven ADR §2 exemption matrix (with `..`-segment fail-closed hardening), `BEEPER_AUTH_MODE` + boot refusals, and SSE mid-stream re-auth. **200 new tests** (store 59, boot 29, resolver 73, SSE 13, session 26) → suite 2294 → **2494**; zero existing tests modified (mode `none` byte-identical — `test_permissions.py` diff empty); no frontend changes; no new dependencies. Security-review gate ran (verdict CONCERNS, no HIGH+): CSRF scheme-comparison fix (MEDIUM, uses `BEEPER_EXTERNAL_SCHEME` per the ADR's own distrust of `request.is_secure` behind TLS-terminating ingress), host case-insensitivity + session `exp` coercion (LOW). Self-review additionally caught a **mode-`none` regression** the reviewer missed: unconditional `SESSION_COOKIE_SECURE=True` would have broken a live Jinja `flash()` cookie over the demo's plain-HTTP port-forward — cookie hardening is now scoped to `local`/`oidc`, with an end-to-end Set-Cookie regression test. ADR ambiguities resolved + the 8.4–8.8 integration contract are recorded in the task report; key seams: `establish_session_identity()`, exempt `/auth/*`+`/api/v1/auth/*` routes must self-enforce CSRF, `adopt_or_create_scim_user()`/`lookup()` for SCIM, `would_orphan_admins()` for the admin UI.
**Task 8.2 — done (2026-08-09, merged `1b35ba5`).** The fail-closed security hotfix. Deleted `_extract_role_from_k8s_token` + call site unconditionally — the unverified-JWT `groups` peek was an **ungated admin bypass live in production** (it ran before 6.2a's `ALLOW_ROLE_HEADER` gate). `[T]` crafted JWT never yields a role in any mode → `test_permissions.py::TestBearerTokensNeverGrantRoles` (13, incl. the exact old bypass payload + the `x.MTIz.y` crash input, in Testing AND Production configs; replaces `TestK8sTokenRoleResolution` with a documented name mapping) · `[T]` chart default `production` / demo pins `development` → `demo/tests/test_demo_automation.py::TestFlaskEnvFailClosed` (5) — orchestrator re-verified via `helm template` on both value sets (only added line each side is the `FLASK_ENV` entry) · `[T]` unknown `FLASK_ENV` fails safe → `::TestGetConfigFailSafeFallback` (7; absent→development per ADR §8, unrecognized→production) · `[T]` suite arithmetic 2282 − 8 + 13 + 7 = **2294**, green on main. `ProductionConfig` docstring corrected (its "admin routes 403 in production" claim was false while the Bearer peek ran first — now true). No Makefile change needed (`demo-beeper` already passes `values-dev.yaml`). 6.2a header behavior untouched.
**Task 8.1 — done (2026-08-09).** `[H]` all four §12 decision points approved by the user **as recommended** (D1 local password accounts; D2 store-primary + default-`user`; D3 production Helm default with demo opt-in; D4 no break-glass); verbatim Q11 directive confirmed (restored to ADR §0 from the conversation record). ADR 0002 → **Accepted**. `[T]` FR54–FR62/NFR25–26 landed → doc-guard `test_route_parity_targets.py` 11/11 green.
**Task 8.2 AC:** `[T]` a crafted 3-segment Bearer token with `{"groups":["beeper-admin"]}` yields `user` (never admin, never 500) under **every** config incl. production — the retained guard asserts the path is gone, not just crash-proofed · `[T]` `test_permissions.py` green with the unverified-JWT pins swapped for named replacements (documented in the PR, never silent) · `[T]` `helm template` demo values (`ui.flaskEnv=development`, `auth.mode=none`) byte-identical to today except `FLASK_ENV`; default values render `FLASK_ENV=production` · `[T]` `make demo-up`/`demo-deploy` pass the development value (Makefile assertion) · `[T]` `get_config("bogus")` → `ProductionConfig`
**Task 8.3 AC:** `[T]` store CRUD, case-insensitive uniqueness (409), adopt-and-link recompute — incl. the named test: local-admin `alice` + SCIM push placing her only in a non-admin group ⇒ resolves `user` · `[T]` per-mode resolver truth table (ADR §1) incl. inactive ⇒ session-cleared 401 and store-miss ⇒ default `user`/strict 403 · `[T]` exemption matrix table-driven per mode (probes/auth/shell/tombstone reachable logged-out; exact 6.2a 410 body preserved) · `[T]` cross-origin POST with valid session ⇒ 403 (single CSRF mechanism) · `[T]` SSE stream terminates within one keepalive tick of deactivation · `[T]` boot refusals (SCIM outside `oidc`; `local`/`oidc` without env SECRET_KEY) fail fast · `[T]` zero-active-admins write raises CRITICAL log + health flag · `[T]` full suite green under `AUTH_MODE=none` (program gate)
**Task 8.4 AC:** `[T]` all 8 clients route through `apiFetch` (static sweep) · `[T]` 401 redirects per mode; mode `none` **never** redirects; probe failure ⇒ treated as `none` (the 87 existing e2e stay green — run them) · `[T]` 403 renders a permission state, not a generic error · `[T]` shared e2e auth fixture exists + static assertion that auth-asserting specs use it
**Task 8.5 AC:** `[T]` full code flow against a stubbed IdP (`responses`-stubbed discovery/JWKS/token — Authlib uses `requests`): valid login ⇒ session+role; bad sig / wrong iss / wrong aud / expired / replayed nonce / tampered state ⇒ no session, never admin · `[T]` group mapping incl. neither-group refusal (SCIM off) and empty-`BEEPER_USER_GROUPS` = any-authenticated · `[T]` open-redirect attempts (`next=https://evil`, `//evil`, encoded variants — decode before validation) rejected · `[T]` cold permalink logged-out → login → identical view (FR53/NFR24), Flask test-client level · `[H]` login/logout UX pass
**Task 8.6 AC:** `[T]` login sets a rotated session; unknown/bad-password/deactivated return identical 401s · `[T]` bootstrap idempotent, never overwrites a rotated password (restart-with-same-secret test); no-seed-no-admin ⇒ startup ERROR + health flag, not crash · `[T]` `local` mode: session admin passes `require_role("admin")`; unauthenticated API ⇒ 401; login page renders outside the shell, axe-clean, story present · `[T]` §9 parity-doc guard accepts net-new blocks and still fails on fabricated citations
**Task 8.7 AC:** `[T]` every route `.required_role == "admin"` (structural) + user-role 403 (behavioral) · `[T]` create/assign/deactivate/reset flows via RTL + one e2e (seeded admin → create user → self-demotion blocked by last-admin 409 rendered inline) · `[T]` SCIM-owned rows read-only with 409 surfaced (mocked linkage) · `[T]` non-admin never sees Manage→Users; direct nav shows in-shell 403 state · `[H]` screenshot review (first net-new view — no Jinja parity target)
**Task 8.8 AC:** `[T]` conformance-by-fixture: golden Okta sequence (dedupe filter → POST → PUT deactivate) + Entra sequence (filter → POST → PATCH profile/active → DELETE) incl. quirk tolerances (op case, string booleans, path-less ops, `excludedAttributes=members`) · `[T]` responses schema-validated against RFC 7643 (dev-dep JSON Schema) · `[T]` auth matrix: no/wrong/secondary token; enabled-tokenless ⇒ 403; disabled ⇒ 404 · `[T]` both membership-PATCH dialects flip the derived role, observable through a gated route within one TTL (fake clock) · `[T]` lifecycle table row-by-row incl. push-vs-login race (login ⇒ `user`, push, ≤60 s ⇒ `admin`, no re-login) and group-rename recompute · `[T]` every mutation audit-logged with fingerprint, token value never logged
**Task 8.9 AC:** `[T]` `helm template` with each mode renders the right env/secret refs from one `ui.auth` tree; default + demo outputs unchanged (snapshot); no secret material in plain values · `[H]` witnessed end-to-end on the kind cluster via port-forward: OIDC login against a real IdP (Keycloak dev instance recommended) **and** a real SCIM provisioning cycle (assign → appears; group-push admin → admin route opens; unassign → 401 within 60 s of the push arriving) **and** the local-mode bootstrap→login→create-user path — each recorded as a protocol sheet (triage-glance-test.md style) · `[H]` deployment-guide + ADR 0001 §8 note merged · `[T]` `make demo-up` unaffected (final program-gate run)

**Parallelizable:** 8.2 immediately (independent hotfix, not blocked on 8.1); 8.1 concurrent with 8.2; 8.3 after both; 8.4 after 8.3; then {8.5, 8.6, 8.8} fan out in parallel (they share only the 8.3 store/resolver and 8.4 seam); 8.7 after 8.6; 8.9 last. Up to 3 concurrent at the widest point.
**Milestone Value:** production regains an admin path for the first time since 6.2a — through verified identity (IdP login + SCIM-governed roles, or admin-managed local accounts), with the spoofable identity sources deleted in every mode and the demo workflow provably untouched.

### Milestone 2.4: Investigation evidence & related knowledge (Q8)

Closes Open Question **Q8**. Design for the non-obvious halves: [RFC 0004](../specs/rfcs/0004-investigation-evidence-exposure.md). Traces to **FR23**, **FR25**, **FR26**, **FR49**, **FR65**, **NFR19**, **NFR21**, **NFR26**, **NFR28**, **NFR30**.

> **Execution order (whole 2026-08-11 expansion).** Four backlog candidates were scoped and reviewed together; the agreed wave order lives in **[docs/plans/main.md](main.md) Phase 7 → "Execution order"** and is not duplicated here. Short version: operator reliability (main.md Phase 7) leads on merit — it is the only candidate whose failure mode is *total*, and it determines whether the demo produces enough **completed** investigations for evidence work to have anything to show. **Milestone 2.4 Tasks 9.1–9.2 run concurrently with it (zero file overlap, verified)**; Milestone 2.5 Task 10.0 also starts in wave 1 as a re-estimation gate. Milestone 2.5 is the **lowest user-visible value and highest LOC** of the four and must not be sequenced first.

| # | Task | Complexity | Dependencies | Status |
|---|------|-----------|--------------|--------|
| 9.0 | **Findings-payload spike (half-day)** — run a prototype `to_step_evidence()` against a real captured findings payload from a demo run; decide whether the Phase-1 blob-parsing path yields evidence an SRE reads as substantiation | S | None | pending |
| 9.1 | **BFF evidence endpoint** — `GET /api/v1/investigations/{id}/evidence`; `to_step_evidence()` + `parse_raw_signal_detail()`; KB hydration from **stored** `exact_match_id`/`relevant_matches`; content-tier split, redaction, caps, short-TTL cache, graceful `available:false` | M | 9.0, [main.md](main.md) 7.0b, RFC 0004 §5 `[H]` | pending |
| 9.2 | **React wiring** — `api/investigation-evidence.ts`, `mergeEvidenceIntoSteps` (keyed on step `key`), refetch policy, populated `RelatedKbPanel` rows + Storybook story | M | 9.1 | pending |
| 9.3 | **Structured signals in the investigator** — `signal_correlation.py` persists a `signals[]` list alongside `raw_signal_detail`; `to_step_evidence()` prefers it. Behind `BEEPER_STRUCTURED_SIGNALS`, **default off** | M | None (parallel with 9.1) | pending |
| 9.4 | **Persistence contract** — deterministic point ids, **corrected read path**, per-step checkpointing, persist-on-exception, persistence-boundary redaction. Behind `BEEPER_CHECKPOINT_FINDINGS`, **default off** | L | 9.3, [main.md](main.md) 7.6 (for the default-on flip) | pending |

**Task 9.0 AC:** `[H]` a written go/adjust decision recorded as a `D`-row before 9.1's ACs are committed — does regex-parsing `_format_signals` produce evidence that reads as substantiation, or as mangled debug output? *(`_format_signals` was written as an **LLM prompt input**, not a UI contract; it lossy-summarizes at 3 series max, `%.2g` precision, and 120-char log truncation. This is the single largest genuine unknown in the milestone, and half a day de-risks 2–3 days.)*

**Task 9.1 AC:** `[T]` `200 {available:false, reason:"not_yet_persisted"}` for an investigation with no Qdrant record; `404` only for an unknown/invalid id · `[T]` `steps` keyed by pipeline step **`key`** (not `order`, which is a render-time index), each item validating against FR25's `{kind:'metric',query,value}` / `{kind:'log',query,excerpt}` shape · `[T]` **content tier (FR65):** a `user` request gets metric evidence and gets log items with `excerpt_suppressed: true`; an `admin` request gets excerpts **only** with `BEEPER_EVIDENCE_RAW_LOGS=true`; in `BEEPER_AUTH_MODE=none` with the flag unset, no excerpt text appears at all · `[T]` a planted-secret corpus (Bearer header, email, AWS-key shape, JWT, long base64 run, `password=` k/v) is absent with the flag on, and `redacted:true` is set · `[T]` the **entropy/key-name backstop** suppresses a high-entropy token matching **no** pattern rule (coverage must not depend solely on the pattern list) · `[T]` a redaction exception drops the item rather than emitting it (fail-closed) · `[T]` redaction rules resolve from a single source, or a parity test asserts the two rule sets are identical · `[T]` `kb_entries` hydrated **only** from stored `exact_match_id`/`relevant_matches` — asserts `EmbeddingService`/`search_semantic` is **never** called (FR26) · `[T]` caps: >8 items/step and >40 total truncate with `truncated:true`; body ≤128 KB · `[T]` a second request within the cache TTL for a `completed` investigation issues **zero** Qdrant calls (the per-worker caveat under multi-worker gunicorn is documented, not implied) · `[T]` structural **and** behavioral authorization, and the 7.0b `/api/v1/*` guard passes with the new route · `[T]` Qdrant unreachable → `200 {available:false, reason:"store_unavailable"}`, never 5xx · `[T]` `investigation_detail_json`'s response equals a **committed golden fixture** (`ui/tests/golden/investigation_detail.json`) — the Task 2.6b backfill-contract guard; "byte-identical to what it was" is not a test unless "what it was" is in the repo · `[O]` p95 endpoint latency measured on the demo and recorded.

**Task 9.2 AC:** `[T]` the summary header renders before any evidence request is issued, and the evidence fetch is not awaited on the first-paint path (NFR19/NFR28) · `[T]` evidence merges onto steps by `key`; a subsequent live `step` event does not clear it (extends the existing `mergeLiveStepEvents` tests) · `[T]` `{available:false}` renders exactly today's absent states — no error, no empty evidence block, "0 Related KB Entries" · `[T]` refetch fires on `complete` and at most once per 5 s otherwise (fake timers); never on every step event and never on a timer · `[T]` a suppressed log excerpt renders an explanatory state, not an empty `<pre>` · `[T]` evidence renders as **text only** — `<code>`/`<pre>` React text children; **no** `content_html`/`render_markdown` treatment (evidence is attacker-influenceable: anyone who can write application logs controls an excerpt) · `[T]` e2e: fixture evidence shows a metric `<code>` and a non-zero Related KB count · `[T]` `lint:terms` clean; Storybook story for the populated `RelatedKbPanel` · `[H]` a reviewer who did not build it confirms the block reads as substantiation, not debug output.

**Task 9.3 AC:** `[T]` with the flag on, `StepResult.data['signals']` has one entry per executed query with `query` preserved **verbatim**; with the flag off, `StepResult.data` is unchanged · `[T]` `to_step_evidence()` prefers `signals` and falls back to `parse_raw_signal_detail()`, with identical output shape · `[T]` the blob parser is pinned by a golden fixture generated from `_format_signals`, and a test fails if `_format_signals` changes without the parser (the parser is **scaffolding with a stated removal trigger**, not a permanent contract) · `[T]` bounded payload (≤50 signals, excerpt ≤1000 chars) · `[O]` a demo run confirms real PromQL text and real log lines reach the endpoint.

**Task 9.4 AC:** `[T]` **idempotence:** N checkpoints for one `investigation_id` leave exactly **one** point, not N · `[T]` **read path:** given a partial checkpoint and a final point for the same id, `get_investigation_findings` returns the **final** one deterministically · `[T]` **pre-existing-data compat:** a legacy random-uuid point loses to a newer deterministic one · `[T]` an exception in `_run_steps`/`_initialize` still leaves completed steps persisted (FR23) · `[T]` with the flag off, the write path is byte-identical to today (one upsert in `_finalize`) · `[T]` `_RESERVED_KEYS` collision protection still applies · `[T]` persistence-boundary redaction applied before the upsert · `[H]` explicit approval to flip the default on, **contingent on [main.md](main.md) Task 7.6** having proved the concurrency cap · `[H]` **NFR26:** `make demo-up` from a clean cluster with zero configuration at the shipped default · `[O]` Qdrant write rate and investigator pod memory measured before/after and recorded.

> **Why 9.4 is L, not M.** The current writer appends a **fresh random UUID point per call** (`agent.py:427`) while the reader does an **unordered `scroll(limit=1)`** (`investigation_service.py:240–250`). Adding checkpoints on top of that is a **regression**, not an addition — the detail view can render a stale partial checkpoint instead of the final result, by UUID luck. Fixing it touches the investigator's persistence contract, its exception path, **and a BFF service**, and needs a data-compat story for the random-uuid points already in every demo and dev Qdrant.

**Parallelizable:** 9.0 first (half a day, gates 9.1's ACs); then 9.1 and 9.3 concurrent; 9.2 after 9.1; 9.4 after 9.3, with its default-on flip after main.md 7.6. Up to 2 concurrent.
**Milestone Value:** the detail view stops being a progress bar — an SRE can see the PromQL that fired, the value it returned, and the prior incident the RCA cited, and decide whether to trust the conclusion.
**Observational Outcomes:** `[O]` evidence presence rate on completed investigations (baseline **0 %**, target ≥80 %); `[O]` Related-KB fidelity against stored step results (baseline **0 %**).

### Milestone 2.5: Completing the Jinja retirement — Phase A (the 10 un-migrated nav routes)

Completes D13's documented Jinja remainder for the **nav destinations**. **No RFC** — see the note below. Amends [docs/design/route-parity-targets.md](../design/route-parity-targets.md) §7. Traces to **FR50**, **NFR20**, **NFR22**, **NFR25**, **NFR26**, **NFR30**.

> **Why no RFC (deliberate, not an omission).** An RFC exists to choose among mechanisms under uncertainty (RFC 0002 spends five sections choosing between persistence and magnitude gating) or to establish an architecture (ADR 0002). Neither applies. The mechanism is fixed and has shipped **five times**: React page + `<domain>_api_bp` at `/api/v1/<domain>` reusing the existing service layer + pytest/RTL/Playwright/axe + a `REACT_OWNED_PREFIXES` flip. Parity targets are pinned and CI-guarded (`test_route_parity_targets.py`). The identity posture is settled by ADR 0002 with a live precedent (AdminUsersPage). There is no open design question an RFC would resolve — what needs writing down are the `D`/`Q` rows in Task 10.0 and the Phase A/Phase B census.

> **Phase A is not full retirement — measured, and this must not be forgotten.** `ui/beeper_ui/routes/` holds **220** `render_template` call sites. The ten nav routes account for **74** (trust_settings 26, services 10, reports 8, notification_config 6, slo 6, spending 6, analytics 4, handoff 4, health 2, topology 2). The other **146** live in `knowledge.py` (**105**) and `investigations.py` (**41**) — the D13/Task-6.3 carve-outs. Of **90** templates, 37 are Phase A, **42** are carve-outs, 11 are shared chrome. There is also a `url_for` obligation: `knowledge.kb_index` and `knowledge.kb_entry` stay registered as redirect stubs purely because ~20 retained templates build hrefs through them. **Completing Phase A deletes 41 % of the templates and 34 % of the render points and leaves `base.html` and HTMX exactly where they are.** Phase B is deferred and **hard-gated behind Milestone 2.4** — it deletes the only surface for data with no JSON equivalent yet.

| # | Task | Complexity | Dependencies | Status |
|---|------|-----------|--------------|--------|
| 10.0 | **Shared scaffolding, §7 amendment & decision rows** — amend `route-parity-targets.md` §7 with the four unpinned sub-routes; record the Phase A/B census; register all 13 React routes + 10 `NAV_GROUPS` entries + one placeholder page each; freeze `App.tsx`/`AppLayout.tsx`; author the e2e route-mock fixture contract; record the `D`/`Q` rows below. **Re-estimation gate for 10.1–10.3** | S | Milestone 2.1 (5.0b precedent) | pending |
| 10.1 | **Batch A — read-only dashboards:** Health, SLO (+ service detail), Topology, Analytics, Handoff. Includes the shared, **data-source-agnostic** `DependencyGraph` lib primitive | L | 10.0 | pending |
| 10.2 | **Batch B — filters, sub-routes, export:** Services (list + detail + feed items; dispose of `/services/<name>/stream`), Reports (executive + noise), Cost Insights (+ CSV) | L | 10.0 | pending |
| 10.3 | **Batch C — mutating admin surfaces:** Notifications channel config + test-send; Trust settings (levels, gates, adaptive tuning, history, apply/reject/evaluate). **Pre-authorized split into 10.3a / 10.3b** | L | 10.0 | pending |
| 10.4 | **Cutover, a11y find-and-fix & template retirement** — extend `REACT_OWNED_PREFIXES` (+9), remove the `/spending/costs` exclusion, **add `/health/api` and `/spending/costs/export` exclusions**, delete 37 templates + dead static JS, extend the axe/keyboard e2e to all 16 destinations, record the Phase B remainder | L | 10.1, 10.2, 10.3 | pending |

**Task 10.0 AC:** `[T]` `ui/tests/test_route_parity_targets.py` passes with §7 amended so that `services/detail.html`, `reports/noise.html`, `slo/service.html`, and `trust/history.html` each resolve to a real file — the guard's `_target_resolves()` already enforces this, so the new rows must be genuine, not decorative · `[T]` `AppLayout.test.tsx` asserts route-derived `activeItemId` for all **16** nav destinations plus the catch-all (today 10 of 16 have **no** active-nav highlight at all — `active_item` defaults to `''` in `components/layout.html`) · `[T]` all 13 placeholder pages render inside the shell; `typecheck`, `lint`, `lint:terms` clean · `[T]` an e2e route-mock fixture **contract** exists (Playwright runs `npm run preview` — the built static bundle with **no Flask** — so every spec supplies data via `page.route`; ~13 new mock corpora are required and each of 10.1–10.3 lands its own with its views, rather than 10.4 discovering the whole corpus at cutover) · `[H]` four decision rows recorded in this plan **before 10.2/10.3 start** (see below) · `[H]` the Phase A/Phase B census (above) merged into `route-parity-targets.md`.

**Task 10.0 — decision rows to record:**
- **D — `/services/<name>/stream` disposition.** It is a BFF poll loop yielding `event: investigation-update` with a rendered `services/_health_feed_items.html` payload, and its only consumer is `services/detail.html`. Task 6.3 set both precedents: the *list-level* investigations stream was **deleted outright**, the *detail* stream was kept because it was excluded from redirect. **Recommend delete.** Note that unlike `investigation_events_json`, `service_health_stream` has **no reauth check** — porting it as-is would ship an **NFR25 regression**; deleting eliminates the risk rather than mitigating it. `[H]`, blocking 10.2.
- **D — Notifications JSON endpoint naming.** `notifications_bp` already owns `/api/v1/notifications` (deliver/digest/audit). New channel-config endpoints go at **`/api/v1/notification-channels`**.
- **D — the auth-posture shift is an *application* of ADR 0002, not a new decision.** After migration, `/app/settings/trust` falls under `EXEMPT_PATH_PREFIXES`'s `/app` entry: the shell renders for anyone who can reach the port and **all** enforcement moves to `/api/v1/trust/*`. Precedented by AdminUsersPage. Recorded so a future reviewer does not re-litigate it — and so it is clear that a missing role decorator is now an unauthenticated data leak where the page gate would previously have caught it (hence NFR30 / main.md 7.0b landing first).
- **Q — is notification test-send's `user` tier still right** once it is a scriptable JSON endpoint? `POST /notifications/test` dispatches a **real** message to a real Slack/PagerDuty/email channel. Parity says keep `user`; security says `admin` plus a per-principal rate limit, because reachability changes even though the gate does not. `[H]`, blocking 10.3.

**Task 10.1 AC:** `[T]` each of the six pages renders every element of its pinned parity template, with error/empty/loading states mirroring the Jinja route's degrade behavior 1:1 (the 5.2 precedent) · `[T]` Analytics `period`/`service` are FR53 permalinks — a cold-loaded seeded URL reproduces the filtered view · `[T]` each new endpoint returns 503 + an error body on backing-service failure and carries `require_role` at or above its Jinja counterpart, asserted structurally via `.required_role` and covered by the 7.0b guard · `[T]` `GET /api/v1/health/status` is a **shape-permissive passthrough** of `HealthService.get_health()` (the 5.2 `ingestion_api_bp` precedent) so operator-side counters added by main.md Phase 7 flow through without an endpoint rewrite — and it is **not** named `/health/api`, which is the k8s probe · `[T]` the `DependencyGraph` lib primitive takes **data-source-agnostic** props (`{nodes:{id,status}[], edges:{from,to}[]}`), never a `topology_service`-shaped payload, and exposes an sr-only tabular equivalent (the 5.5 finding-7 precedent); it has a Storybook story · `[T]` the `GET /api/v1/topology/` docstring records that it is **provisional** — its data source swaps to the operator graph if main.md Task 8.2 lands · `[T]` axe clean on all six · `[H]` screenshot review of Topology, the only view with no Jinja-analogous React precedent.

**Task 10.2 AC:** `[T]` Services list + detail at parity; feed items load and refresh without manual reload · `[T]` **the SSE disposition is executed and proven:** if deleted, `/services/<name>/stream` returns 404 and no template references it; if ported, the JSON stream terminates within 60 s of role loss (NFR25, mirroring the investigations reauth test) · `[T]` Cost Insights CSV export reaches `/spending/costs/export` with current filters and returns `text/csv` + `Content-Disposition` (the 5.4 dev-only vite-proxy gap is documented, not "fixed") · `[T]` `/reports/noise` has a React page — not silently dropped · `[T]` input validation carried over, not re-derived: each new endpoint reuses the **same** pattern object from its module (`analytics.py`'s `_SERVICE_NAME_RE`, `spending.py`'s `_validate_cost_filters` and `VALID_FORMATS` allowlist) · `[O]` after 10.4, no 5xx on `/api/v1/services/*` over a full `make demo-up` cycle.

**Task 10.3 AC:** `[T]` **every new Trust mutation endpoint has `.required_role == "admin"` (structural) and returns 403 for a `user` session (behavioral)** — the `admin_users.py` two-axis pattern; the single most important test in this milestone · `[T]` a `user` session sees no admin affordance **and** a direct API call from that session still 403s (client hiding is not enforcement) · `[T]` unauthenticated direct nav to `/app/settings/trust` renders the in-shell 403/login state, never a blank shell or partial data · `[T]` a JSON mutation with a cross-origin `Origin` header is rejected by `resolve_request_identity()`'s existing same-origin check — inherited, but each new unsafe-method route needs its own assertion, because inheritance without a test is how this regresses · `[T]` the test-send endpoint implements the 10.0 `[H]` decision (tier + per-principal rate limit) and its docstring records the outbound-message amplification · `[T]` new Trust endpoints follow `trust_settings.py`'s own construction pattern (`os.getenv("QDRANT_HOST"/"QDRANT_PORT")`, **not** `current_app.config` like `HealthService`/`SpendingService`) or the test fixtures will not bind · `[H]` review of the Trust destructive-action confirmation UX (adaptive apply/reject have **no undo**).

> **10.3 is not a repeat, and the split is pre-authorized.** `trust_settings.py` is 422 lines with **26** `render_template` calls — more than the other nine nav routes combined (48 total). It owns 10 templates, three backing services, and **5** `@require_role("admin")` mutations. Roughly half its API already exists (`trust_config_bp`, `confidence_gates_bp` cover trust-level and gate-threshold list/get/PUT); what has **no** JSON equivalent is the adaptive-tuning half. **Pre-authorized split: 10.3a** (Notifications + Trust read/levels/gates) **/ 10.3b** (adaptive tuning + the 5 admin mutations) — pre-authorizing converts an overrun into a planned boundary rather than a silent descope.

**Task 10.4 AC:** `[T]` every one of the 16 nav destinations 302s its bare URL to the correct `/app/*` target with query string preserved (extends `test_jinja_retirement.py` against the real `Config.REACT_OWNED_PREFIXES`), and the redirect-chain test **follows** the 302 and asserts the React view actually renders (the `TestAC3RedirectChainReachesCorrectReactView` precedent — this is what catches a Cost Insights cutover mistake, whose mechanics are *inverse*: it migrates by **removing** an exclusion, not by appending a prefix) · `[T]` **`GET /health/api` still returns 200 and is NOT redirected** — the k8s-probe regression guard, and the single highest-consequence line in the milestone; note `REACT_OWNED_PREFIXES` currently holds the narrow `/health/ingestion`, so broadening it to `/health` makes `/health/api` match, and the cutover must also decide whether the narrow entry is replaced or left alongside · `[T]` `/spending/costs/export` and `/metrics/export` still return CSV, unredirected · `[T]` no orphaned template includes after the 37 deletions (static regex, the existing `test_no_orphaned_template_includes_after_deletion` precedent) · `[T]` **`url_for` integrity as a static test:** regex every `url_for('<endpoint>'` occurrence across retained templates and assert each endpoint exists in `app.url_map`. *(Deliberately **not** a render sweep — rendering ~53 retained templates blind raises `UndefinedError` on missing context long before reaching a `url_for`, and Jinja's default `Undefined` masks exactly the endpoint references being checked. The static form is genuinely writable and directly guards the `knowledge.kb_index`/`kb_entry` stub obligation.)* · `[T]` axe (WCAG 2.0/2.1 A+AA) + keyboard/focus e2e cover all 16 destinations · `[T]` **NFR26 unchanged:** `helm template` output byte-identical under default and demo values against a committed golden manifest; `make demo-up` needs zero new configuration · `[T]` a per-blueprint `render_template` census reaches **0** for the ten nav blueprints (baseline 74) · `[T]` a guard asserting the **Phase B remainder is still present and accounted for** — `base.html`, `htmx.min.js`, and the 42 carve-out templates still exist, with a doc citation, so a future reader cannot mistake Phase A for full retirement · `[H]` the plan and `route-parity-targets.md` state plainly that `base.html`/HTMX deletion remains blocked on the KB + investigations carve-outs.

> **10.4 is an L, not an M.** It flips 9 prefixes, removes 1 exclusion, adds 2 exclusions, deletes 37 templates, sweeps for orphaned includes and `url_for` breakage across every retained template, and runs a **find-and-fix** axe/keyboard audit across 10 net-new destinations. Task 5.5 was a full M for **5** routes and found **8 real bugs**, several serious. This is where the schedule risk lives.

**Parallelizable:** 10.0 first and alone (it is a re-estimation gate — its §7 amendment found **four** unpinned sub-routes and 12 templates beyond what §7 names, and `trust_settings.py`'s 26 render points may hide more); then 10.1–10.3 concurrent, bound by the 5.0b file contract (`App.tsx`/`AppLayout.tsx` **frozen**; `src/lib/index.ts` and `routes/__init__.py` additive-only — Milestone 2.1's post-merge note documents the barrel conflict as *expected* and resolvable by keeping all blocks); 10.4 last. Up to 3 concurrent.
**Milestone Value:** one nav shell, one rendering stack, one accessibility audit surface across all 16 destinations — no more full page reload and lost sidebar highlight when crossing between the React and Jinja halves.
**Observational Outcomes:** `[O]` `render_template` call sites 220 → 146; template files 90 → 53; nav destinations served by React 6/16 → 16/16; nav destinations under the axe/keyboard audit 6/16 → 16/16.
**Deferred — Phase B (not scheduled here):** the 146 remaining `render_template` calls in `knowledge.py` (105) and `investigations.py` (41), 42 carve-out templates, `base.html`, and HTMX. **Hard-gated behind Milestone 2.4:** do **not** delete `evidence_service.py` or `kb_surfacing_service.py` — after Phase B removes the HTML detail SSE, `get_timeline_events()` loses its last caller, and deleting it before Task 9.1 would destroy the extraction logic 9.1 builds on. Also: `investigation_related_kb` **violates FR26** and must not be preserved as parity, and `confirm_resolution` writes `"resolution_confirmed_by": "sre"` as a hardcoded literal — the only action class that mutates incident state currently produces **no accountability record**, and whichever batch migrates those routes must write the real principal from `g.user`.

## Phase 3: Blast Radius (Gated — RFC 0001 Phase 3, now designed as [RFC 0005](../specs/rfcs/0005-cross-service-incident-correlation.md) / [main.md](main.md) Phase 8)

### Milestone 3.1: Cross-service impact

| # | Task | Complexity | Dependencies | Status |
|---|------|-----------|--------------|--------|
| 7.1 | Incident-detail blast-radius (upstream/downstream correlated services), replacing the placeholder | M | [main.md](main.md) Task 8.3 (operator emits correlation) **and** Task 10.1 (`DependencyGraph` primitive) | blocked |

**Task 7.1 AC:** `[T]` detail renders correlated upstream/downstream services once the operator exposes correlation fields, replacing the "not yet correlated" placeholder shipped in Task 2.5 (FR48)

**Task 7.1 — dependency reconciled (2026-08-11).** "RFC 0001 Phase 3 (external)" is now concrete: RFC 0001 Phase 3 is designed in **[RFC 0005](../specs/rfcs/0005-cross-service-incident-correlation.md)** and planned as **[docs/plans/main.md](main.md) Phase 8**. **Task 7.1 *is* main.md Task 8.4** — the same unit of work, tracked in one place (main.md, because the bulk is operator REST + BFF plumbing) and mirrored here so this plan's only `blocked` row resolves. Three consequences:

- **It stays `blocked`, but on named work rather than an external unknown.** It unblocks when main.md Task 8.3 lands (the operator writes `status.correlations`/`correlation_group`/`correlated_services`/`blast_radius`), which itself is gated on Task 8.1a's **go/no-go graph-density measurement**. If 8.1a returns **no-go**, the correct outcome is to close 7.1 as **won't-do** and leave FR48 suppressed — that is a result, not a failure (RFC 0005 §8 Alt A).
- **Complexity re-sized L → M.** The frontend contract is already pinned (`correlated_services?: string[]` exists at `investigation-detail.ts:109`; D1's render-nothing-when-absent rule is regression-guarded at `InvestigationDetailPage.test.tsx:283`), and the graph visual comes from Task 10.1's shared primitive rather than being built here. What remains is the panel, the SSE metadata-diff path (net-new — `_generate_json_sse_events` does not diff arbitrary metadata fields today), the Storybook story, and the role split on the service-name list. The L now lives in main.md Tasks 8.2/8.3.
- **Binding contract with Task 10.1:** `BlastRadiusPanel` **composes** `src/lib/components/DependencyGraph/`; it does not reimplement it. Full ACs are in main.md Task 8.4.

**Parallelizable:** none — blocked on [main.md](main.md) Task 8.3 (which is itself gated on Task 8.1a's go/no-go) and on Task 10.1's shared primitive.
**Milestone Value:** SREs see incident scope (blast radius) at a glance.
**Observational Outcomes:** `[O]` faster scoping of multi-service incidents.
