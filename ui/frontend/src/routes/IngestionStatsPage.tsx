/**
 * IngestionStatsPage — route scaffolding placeholder (Task 5.0b).
 *
 * Placeholder — Task 5.2 replaces this file's implementation with the real
 * Ingestion/Detection Stats view (`/app/ingestion-stats`, per
 * docs/design/route-parity-targets.md §3). Renders only an honest
 * "not yet migrated" notice inside the shell — nothing here should be
 * mistaken for the finished feature.
 */
export function IngestionStatsPage() {
  return (
    <div data-testid="ingestion-stats-page" className="flex flex-col gap-2">
      <h1 className="text-2xl font-semibold text-text-primary">Ingestion Stats</h1>
      <p className="text-sm text-text-secondary">This view has not been migrated to React yet.</p>
    </div>
  )
}
