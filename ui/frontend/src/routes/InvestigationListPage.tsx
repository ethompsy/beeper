/**
 * InvestigationListPage — Task 2.1 route placeholder.
 *
 * Minimal content only: the real investigation list (rows, filters,
 * skeleton, scroll restoration) is Task 2.2's scope. This page exists so
 * the shell has something to route to at `/investigations` and so the
 * focus-management tests have a concrete "list route" to navigate back to.
 */
export function InvestigationListPage() {
  return (
    <div data-testid="investigation-list-page">
      <h1 className="text-2xl font-semibold text-text-primary">Investigations</h1>
      <p className="mt-2 text-text-secondary">List view placeholder — Task 2.2 builds this out.</p>
    </div>
  )
}
