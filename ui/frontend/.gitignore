# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-lib
dist-ssr
*.local
storybook-static

# Playwright (Task 1.7)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# The root repo .gitignore has a Python-venv-oriented `lib/` rule that
# otherwise shadows our React component library at src/lib/ — un-ignore it
# explicitly (Task 1.4).
!src/lib/
!src/lib/**

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
