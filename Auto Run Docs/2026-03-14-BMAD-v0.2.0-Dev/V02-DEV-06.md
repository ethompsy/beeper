# Phase 06: Epic 6 — Knowledge & Signal Intelligence (Wave 3)

This phase completes Wave 3 by implementing Epic 6 (9 stories): automatic KB entry creation from resolved investigations, bi-directional KB-investigation links, per-service knowledge views, KB entry validation weighting, KB entry review/edit/correction, unified investigation timeline, deploy correlation, service dependency topology, and change event ingestion & correlation. By the end, the KB compounds automatically and Beeper correlates anomalies with deploys, config changes, and service topology.

## Tasks

- [x] Create story 6-1 spec (Automatic KB Entry Creation from Resolved Investigations) using the `/bmad-bmm-create-story` skill from Epic 6 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [x] Code review story 6-1 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `6-1-automatic-kb-entry-creation: done`. Update story file status to `done`. Commit as `6-1 done`. Make all decisions autonomously.
  - Review found 3 MEDIUM + 2 LOW issues. Auto-fixed: empty resolution field extraction, dead enrichment resolution code, broad type hint. Added 2 new tests. All 3,072 tests pass (916 investigator + 1,625 UI + 531 operator).

- [ ] Create story 6-2 spec (Bi-Directional KB-Investigation Links) using the `/bmad-bmm-create-story` skill from Epic 6 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 6-2 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `6-2-bi-directional-kb-investigation-links: done`. Update story file status to `done`. Commit as `6-2 done`. Make all decisions autonomously.

- [ ] Create story 6-3 spec (Per-Service Knowledge Views) using the `/bmad-bmm-create-story` skill from Epic 6 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 6-3 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `6-3-per-service-knowledge-views: done`. Update story file status to `done`. Commit as `6-3 done`. Make all decisions autonomously.

- [ ] QA checkpoint: Run the full test suite across ALL components (investigator pytest + ruff + mypy, UI pytest, operator cargo test). Report total test counts per component. If any regressions exist, fix them and commit as `fix: QA checkpoint regression`. Make all decisions autonomously.

- [ ] Create story 6-4 spec (KB Entry Validation Weighting) using the `/bmad-bmm-create-story` skill from Epic 6 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 6-4 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `6-4-kb-entry-validation-weighting: done`. Update story file status to `done`. Commit as `6-4 done`. Make all decisions autonomously.

- [ ] Create story 6-5 spec (KB Entry Review, Edit & Correction) using the `/bmad-bmm-create-story` skill from Epic 6 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 6-5 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `6-5-kb-entry-review-edit-correction: done`. Update story file status to `done`. Commit as `6-5 done`. Make all decisions autonomously.

- [ ] Create story 6-6 spec (Unified Investigation Timeline) using the `/bmad-bmm-create-story` skill from Epic 6 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 6-6 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `6-6-unified-investigation-timeline: done`. Update story file status to `done`. Commit as `6-6 done`. Make all decisions autonomously.

- [ ] QA checkpoint: Run the full test suite across ALL components (investigator pytest + ruff + mypy, UI pytest, operator cargo test). Report total test counts per component. If any regressions exist, fix them and commit as `fix: QA checkpoint regression`. Make all decisions autonomously.

- [ ] Create story 6-7 spec (Deploy Correlation) using the `/bmad-bmm-create-story` skill from Epic 6 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 6-7 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `6-7-deploy-correlation: done`. Update story file status to `done`. Commit as `6-7 done`. Make all decisions autonomously.

- [ ] Create story 6-8 spec (Service Dependency Topology) using the `/bmad-bmm-create-story` skill from Epic 6 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 6-8 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `6-8-service-dependency-topology: done`. Update story file status to `done`. Commit as `6-8 done`. Make all decisions autonomously.

- [ ] Create story 6-9 spec (Change Event Ingestion & Correlation) using the `/bmad-bmm-create-story` skill from Epic 6 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 6-9 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `6-9-change-event-ingestion-correlation: done`. Update story file status to `done`. Commit as `6-9 done`. Make all decisions autonomously.

- [ ] Run Epic 6 retrospective using the `/bmad-bmm-retrospective` skill. Create retrospective file at `_bmad-output/implementation-artifacts/epic-6-retro-2026-MM-DD.md` (use today's date). Update sprint-status.yaml: `epic-6: done`, `epic-6-retrospective: done`. Commit as `epic-6 retrospective done`. Make all decisions autonomously.
