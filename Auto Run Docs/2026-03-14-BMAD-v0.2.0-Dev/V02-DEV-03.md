# Phase 03: Epic 3 — Graduated Trust & Autonomy (Wave 2)

This phase begins Wave 2. It starts with a full environment validation, then implements Epic 3 (7 stories): trust level configuration, confidence gate engine, confidence gate threshold configuration, one-click investigation feedback, adaptive alert threshold tuning, impact-weighted escalation urgency, and noise report dashboard. By the end, admins can control Beeper's autonomy per service and track accuracy over time.

## Tasks

- [x] Wave 2 environment validation. Run all test suites across ALL components (investigator pytest + ruff + mypy, UI pytest, operator cargo test). Verify all tests from Wave 1 still pass. If any failures exist, fix them and commit as `fix: wave-2 pre-flight`. Report total test counts per component. Make all decisions autonomously.
  - **Results — all Wave 1 tests pass, no regressions:**
    - Investigator pytest: **517 passed**, 3 skipped
    - Investigator ruff: **All checks passed**
    - Investigator mypy: **No issues found** (26 source files)
    - UI pytest: **1,030 passed**
    - Operator cargo test: **cargo unavailable locally** (Rust toolchain not installed — consistent with Wave 1; operator Rust code is compiled/tested in CI/Docker)
  - No fixes needed — no `fix: wave-2 pre-flight` commit required.

- [x] Create story 3-1 spec (Trust Level Configuration & Persistence) using the `/bmad-bmm-create-story` skill from Epic 3 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.
  - **Results — story 3-1 created and implemented:**
    - Created comprehensive story spec with exhaustive artifact analysis
    - Implemented TrustLevelService with TL1-5 per-service autonomy configuration
    - API routes: GET/PUT /api/v1/trust/services, GET /api/v1/trust/definitions
    - UI settings page: /settings/trust/ with HTMX inline updates
    - Permission enforcement: user reads, admin writes (NFR12)
    - Uses existing `service_trust_levels` Qdrant collection with `autonomy_*` fields
    - 70 new tests (31 service + 24 API + 15 UI), all passing
    - Full UI suite: 1100 passed, zero regressions
    - Investigator: 517 passed, 3 skipped (unchanged)
    - Ruff + mypy: all clean

- [x] Code review story 3-1 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `3-1-trust-level-configuration-persistence: done`. Update story file status to `done`. Commit as `3-1 done`. Make all decisions autonomously.
  - **Results — code review completed, 5 MEDIUM + 1 LOW issues found and auto-fixed:**
    - Fixed boolean bypass in trust level validation (True/False accepted as valid levels)
    - Removed redundant `except (UnexpectedResponse, Exception)` clauses (3 occurrences)
    - Added "localhost" default to `os.getenv("QDRANT_HOST")` in both route files
    - Added service name validation (regex + length) to API PUT route
    - Pre-compiled regex pattern as module-level constant
    - Added 6 new tests (2 boolean rejection in service, 2 boolean rejection in API, 2 service name validation in API)
    - All 76 trust tests pass (70 original + 6 new)
    - Full UI suite: 1106 passed, zero regressions
    - Investigator: 505 passed, 12 pre-existing LLM client test failures (confirmed identical without changes)
    - Ruff: all story 3-1 files clean

- [x] Create story 3-2 spec (Confidence Gate Engine) using the `/bmad-bmm-create-story` skill from Epic 3 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.
  - **Results — story 3-2 created and implemented:**
    - Created comprehensive story spec with exhaustive artifact analysis
    - Implemented ConfidenceGateService with TL1-5 gate evaluation and composite scoring
    - Gate thresholds: TL3=90%, TL4=85%, TL5=80%; TL1-2 always advisory
    - Confidence score normalization: RCA percentage, text levels, KB boost, composite weighting
    - API routes: POST /api/v1/trust/gates/evaluate, GET/PUT /api/v1/trust/gates/<level>
    - UI integration: HTMX gate-status partial on investigation detail page
    - Sentinel key `__gate_thresholds__` in existing Qdrant collection for threshold persistence
    - 64 new tests (43 service + 15 API + 6 UI), all passing
    - Full UI suite: 1170 passed, zero regressions
    - Ruff: all clean after auto-fix

- [x] Code review story 3-2 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `3-2-confidence-gate-engine: done`. Update story file status to `done`. Commit as `3-2 done`. Make all decisions autonomously.
  - **Results — code review completed, 3 MEDIUM + 2 LOW issues found and auto-fixed:**
    - Moved inline `import os` from function body to module-level imports in `investigations.py`
    - Added `ConfidenceGateError` import and proper exception logging in `investigation_gate_status` (replaced silent bare `except Exception` with specific `ConfidenceGateError` catch + logged fallback `Exception` catch)
    - Removed dead helper function `_mock_trust_service` from `test_confidence_gate_service.py`
    - Added boolean confidence_score rejection test for POST evaluate route
    - Added boolean threshold rejection test for PUT update route
    - 2 new tests added (boolean rejection in API routes)
    - Full UI suite: 1172 passed (1170 + 2 new), zero regressions
    - Investigator: 505 passed, 12 pre-existing LLM client async test failures (identical without changes)
    - Ruff: all story 3-2 files clean

- [x] Create story 3-3 spec (Confidence Gate Threshold Configuration) using the `/bmad-bmm-create-story` skill from Epic 3 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.
  - **Results — story 3-3 created and implemented:**
    - Created comprehensive story spec with exhaustive artifact analysis
    - Implemented gate threshold configuration UI on existing `/settings/trust/` page
    - HTMX lazy-loaded "Confidence Gate Thresholds" section with TL3/TL4/TL5 cards
    - UI routes: GET /settings/trust/gates (display), POST /settings/trust/gates/<tl>/update (admin)
    - Reused story 3-2's ConfidenceGateService backend — no new API routes or dependencies
    - Validation: rejects non-numeric, out-of-range, and advisory-only TL1-2 configurations
    - `@require_role("user")` for viewing, `@require_role("admin")` for updating (NFR12)
    - 23 new tests (9 GET section + 14 POST update), all passing
    - Full UI suite: 1195 passed, zero regressions
    - Ruff: all new/modified Python files clean

- [x] Code review story 3-3 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `3-3-confidence-gate-threshold-configuration: done`. Update story file status to `done`. Commit as `3-3 done`. Make all decisions autonomously.
  - **Results — code review completed, 3 MEDIUM + 2 LOW issues found and auto-fixed:**
    - Fixed dead boolean validation: `isinstance(threshold_str, bool)` was checking a string variable (always False); replaced with proper string-based boolean check (`threshold_str.strip().lower() in ("true", "false")`) before float conversion
    - Aligned HTML form `min="0.01"` → `min="0.0"` in both `_gate_thresholds.html` and `_gate_update_result.html` to match server-side 0.0-1.0 validation and AC specification
    - Added 2 new boolean string rejection tests (`test_rejects_boolean_true_threshold`, `test_rejects_boolean_false_threshold`)
    - 2 LOW issues noted (error card lacks retry form, no min-threshold warning) — not blocking
    - Full UI suite: 1197 passed (1195 + 2 new), zero regressions
    - Investigator: 505 passed, 12 pre-existing LLM client async test failures (identical without changes)
    - Ruff: all story 3-3 files clean

- [x] Create story 3-4 spec (One-Click Investigation Feedback) using the `/bmad-bmm-create-story` skill from Epic 3 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.
  - **Results — story 3-4 created and implemented:**
    - Created comprehensive story spec with exhaustive artifact analysis
    - Implemented one-click investigation feedback: accurate / inaccurate / not-an-issue
    - POST /investigations/<id>/feedback route with @require_role("user") — any authenticated user
    - Single-click HTMX buttons (no modal, no form, no confirmation) with hx-vals for form-less submission
    - Feedback stored in Qdrant investigations collection via existing save_resolution_feedback()
    - Qdrant payload keys: investigation_feedback, investigation_feedback_by, investigation_feedback_at
    - Feedback section integrated in _detail_content.html after Confidence Gate, before Resolution Confirmation
    - Selected feedback highlighted via .feedback-btn-selected CSS class; last feedback wins
    - SSE feedback-update event for real-time cross-viewer updates
    - X-Beeper-User header for identity tracking; defaults to "anonymous"
    - 19 new tests (14 route + 5 integration), all passing
    - Full UI suite: 1216 passed, zero regressions
    - Ruff: all clean after import sort fix

- [x] Code review story 3-4 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `3-4-one-click-investigation-feedback: done`. Update story file status to `done`. Commit as `3-4 done`. Make all decisions autonomously.
  - **Results — code review completed, 3 MEDIUM + 2 LOW issues found and auto-fixed:**
    - Added missing `except InvestigationServiceError` handler in `submit_investigation_feedback()` — Qdrant failure now returns graceful 503 instead of unhandled 500 (consistency with all other POST routes)
    - Refactored `_feedback_result.html` to `{% include %}` `_feedback_buttons.html` instead of duplicating 29 lines of button markup (DRY fix)
    - Strengthened `test_feedback_records_timestamp` — replaced weak string presence checks with `datetime.fromisoformat()` + `tzinfo is not None` validation
    - Added `test_feedback_qdrant_failure_returns_503` test for the new error handling
    - 2 LOW issues noted (error template loses highlight state, no aria-pressed on buttons) — not blocking
    - Full UI suite: 1217 passed (1216 + 1 new), zero regressions
    - Investigator: 517 passed, 3 skipped (unchanged)
    - Ruff + mypy: all clean

- [ ] QA checkpoint: Run the full test suite across ALL components (investigator pytest + ruff + mypy, UI pytest, operator cargo test). Report total test counts per component. If any regressions exist, fix them and commit as `fix: QA checkpoint regression`. Make all decisions autonomously.

- [ ] Create story 3-5 spec (Adaptive Alert Threshold Tuning) using the `/bmad-bmm-create-story` skill from Epic 3 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 3-5 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `3-5-adaptive-alert-threshold-tuning: done`. Update story file status to `done`. Commit as `3-5 done`. Make all decisions autonomously.

- [ ] Create story 3-6 spec (Impact-Weighted Escalation Urgency) using the `/bmad-bmm-create-story` skill from Epic 3 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 3-6 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `3-6-impact-weighted-escalation-urgency: done`. Update story file status to `done`. Commit as `3-6 done`. Make all decisions autonomously.

- [ ] Create story 3-7 spec (Noise Report Dashboard) using the `/bmad-bmm-create-story` skill from Epic 3 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 3-7 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `3-7-noise-report-dashboard: done`. Update story file status to `done`. Commit as `3-7 done`. Make all decisions autonomously.

- [ ] Run Epic 3 retrospective using the `/bmad-bmm-retrospective` skill. Create retrospective file at `_bmad-output/implementation-artifacts/epic-3-retro-2026-MM-DD.md` (use today's date). Update sprint-status.yaml: `epic-3: done`, `epic-3-retrospective: done`. Commit as `epic-3 retrospective done`. Make all decisions autonomously.
