# Phase 01: Epic 1 — SLO Platform & Permissions Foundation (Wave 1)

This phase begins v0.2.0 development. It starts with committing all outstanding v0.2.0 planning artifacts and validating the development environment, then implements Epic 1 (8 stories): permission model, secrets/PII scrubbing, ServiceLevel CRD, SLO burn rate calculation, customer impact scoring, error budget policies, SLO compliance dashboard, and platform resilience. By the end, the SLO platform and permission foundation are operational.

## Tasks

- [x] Commit all outstanding v0.2.0 planning artifacts. Stage and commit: modified `_bmad-output/planning-artifacts/prd.md`, `_bmad-output/planning-artifacts/architecture.md`, `_bmad-output/planning-artifacts/epics.md`, `_bmad-output/implementation-artifacts/sprint-status.yaml`, and all new untracked files in `_bmad-output/planning-artifacts/`, `_bmad-output/brainstorming/`, `_bmad-output/test-design-architecture.md`, `_bmad-output/test-design-qa.md`, and `whats-next.md`. Do NOT commit `.history/` files. Commit with message: `v0.2.0 planning artifacts`. Make all decisions autonomously.
  > ✅ Committed 12 files (4 modified, 8 new) as `a73bffc`. Pushed to main.

- [x] Validate development environment and fix any pre-existing issues. Run all test suites across all components: investigator (`cd investigator && poetry install && poetry run pytest && poetry run ruff check . && poetry run mypy .`), UI (`cd ui && poetry install && poetry run pytest`), operator (`cd operator && cargo test` if cargo is available). If any tests fail, fix them. If any lint or type issues exist, fix them. Commit fixes as `fix: resolve pre-existing issues` if any changes were needed. Verify: all tests pass before proceeding. Make all decisions autonomously.
  > ✅ All suites validated. Investigator: 406 passed, 3 skipped; ruff clean; mypy clean. UI: 626 passed. Operator: cargo not available, skipped. Fixed 19 mypy errors (strict mode) and 1 ruff issue across `demo.py` and `test_investigation_documentation.py`. Committed as `8f9dd61`, pushed to main.

- [x] Create story 1-1 spec (Permission Model Enforcement) using the `/bmad-bmm-create-story` skill from Epic 1 in `_bmad-output/planning-artifacts/epics.md`. Update sprint-status.yaml. Make all decisions autonomously.
  > ✅ Story file created at `_bmad-output/implementation-artifacts/1-1-permission-model-enforcement.md`. Sprint-status.yaml updated: epic-1 → in-progress, 1-1-permission-model-enforcement → ready-for-dev. Comprehensive story includes 5 tasks with subtasks, architecture compliance patterns, RFC 7807 error format, guardrails preventing regression on 626 existing tests, and full reference citations.

- [x] Implement story 1-1 (Permission Model Enforcement) using the `/bmad-bmm-dev-story` skill. Make all decisions autonomously.
  > ✅ Implemented permission middleware with `require_role()` decorator and `before_request` role resolver. 3-step role resolution: K8s JWT → X-Beeper-Role header → default "user". RFC 7807 error responses for 403. 28 new tests across 10 test classes. All 654 tests pass (626 existing + 28 new), zero regressions. Ruff and mypy clean.

- [x] Code review story 1-1 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `1-1-permission-model-enforcement: done`. Update story file status to `done`. Commit as `1-1 done`. Make all decisions autonomously.
  > ✅ Adversarial review found 5 issues (1 HIGH, 2 MEDIUM, 2 LOW). **HIGH: Security fix** — valid K8s JWT without admin group was falling through to X-Beeper-Role header, allowing privilege escalation; now returns "user" authoritatively. **MEDIUM:** Added `require_role()` argument validation (rejects invalid roles at decoration time); added `patch` method and proper type annotations to `_RoleClient`. **LOW:** Improved weak test to actually verify `g.user_role`; added bypass security test. All fixes auto-applied. 3 new tests added (31 total new). All 657 UI tests pass, 406 investigator tests pass (3 skipped). Ruff/mypy clean on changed files.

- [x] Create story 1-2 spec (Secrets Management & PII Scrubbing) using the `/bmad-bmm-create-story` skill from Epic 1 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.
  > ✅ Story file created at `_bmad-output/implementation-artifacts/1-2-secrets-management-pii-scrubbing.md`. Implemented `PiiScrubber` in `investigator/beeper_investigator/llm/scrubber.py` with 11 default regex rules (emails, IPv4/IPv6, JWT, Bearer tokens, API keys for OpenAI/GitHub/Slack, AWS keys, passwords, credential URLs). Integrated into `LlmClient.complete_sync()`, `complete()`, and `embed_sync()` — scrubs all messages/text before any LiteLLM call. Deep copy preserves originals. Audit logging via structured JSON (INFO summary + DEBUG detail). Configurable custom rules via `BEEPER_SCRUB_RULES_JSON` env var + runtime `add_rule()`/`remove_rule()`. K8s Secrets credential pattern validated and documented (NFR10). 74 new tests across 14 test classes. All 480 investigator tests pass (406 existing + 74 new), all 657 UI tests pass. Ruff/mypy clean. Committed as `f1c4dee`, pushed to main.

- [x] Code review story 1-2 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `1-2-secrets-management-pii-scrubbing: done`. Update story file status to `done`. Commit as `1-2 done`. Make all decisions autonomously.
  > ✅ Adversarial review found 5 issues (0 HIGH, 3 MEDIUM, 2 LOW). **MEDIUM:** Removed unnecessary `reversed()` + misleading comment in `scrub_text()` (audit entries were created in reverse order for no reason); removed dead code in `test_complete_sync_scrubs_before_llm` (unused `sent_messages` variable); added proper return type to `_make_client()` replacing `# type: ignore[no-untyped-def]`. **LOW:** Added test for `investigation_id` in audit log output; added K8s YAML false-positive test (`secretName:`, `secretRef:` not falsely scrubbed). All fixes auto-applied. 2 new tests added (76 total scrubber tests). All 482 investigator tests pass (3 skipped), all 657 UI tests pass. Ruff/mypy clean.

- [ ] Create story 1-3 spec (ServiceLevel CRD & Controller) using the `/bmad-bmm-create-story` skill from Epic 1 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 1-3 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `1-3-servicelevel-crd-controller: done`. Update story file status to `done`. Commit as `1-3 done`. Make all decisions autonomously.

- [ ] Create story 1-4 spec (SLO Burn Rate Calculation Engine) using the `/bmad-bmm-create-story` skill from Epic 1 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 1-4 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `1-4-slo-burn-rate-calculation-engine: done`. Update story file status to `done`. Commit as `1-4 done`. Make all decisions autonomously.

- [ ] QA checkpoint: Run the full test suite across ALL components (investigator pytest + ruff + mypy, UI pytest, operator cargo test). Report total test counts per component. If any regressions exist, fix them and commit as `fix: QA checkpoint regression`. Make all decisions autonomously.

- [ ] Create story 1-5 spec (Customer Impact Scoring & Investigation Priority) using the `/bmad-bmm-create-story` skill from Epic 1 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 1-5 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `1-5-customer-impact-scoring-investigation-priority: done`. Update story file status to `done`. Commit as `1-5 done`. Make all decisions autonomously.

- [ ] Create story 1-6 spec (Error Budget Policies) using the `/bmad-bmm-create-story` skill from Epic 1 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 1-6 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `1-6-error-budget-policies: done`. Update story file status to `done`. Commit as `1-6 done`. Make all decisions autonomously.

- [ ] Create story 1-7 spec (SLO Compliance Dashboard) using the `/bmad-bmm-create-story` skill from Epic 1 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 1-7 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `1-7-slo-compliance-dashboard: done`. Update story file status to `done`. Commit as `1-7 done`. Make all decisions autonomously.

- [ ] Create story 1-8 spec (Platform Resilience) using the `/bmad-bmm-create-story` skill from Epic 1 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 1-8 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `1-8-platform-resilience: done`. Update story file status to `done`. Commit as `1-8 done`. Make all decisions autonomously.

- [ ] Run Epic 1 retrospective using the `/bmad-bmm-retrospective` skill. Create retrospective file at `_bmad-output/implementation-artifacts/epic-1-retro-2026-MM-DD.md` (use today's date). Update sprint-status.yaml: `epic-1: done`, `epic-1-retrospective: done`. Commit as `epic-1 retrospective done`. Make all decisions autonomously.
