# Phase 07: Epic 7 — Developer Experience & Analytics (Wave 4)

This phase begins Wave 4. It starts with a full environment validation, then implements Epic 7 (7 stories): command palette & keyboard shortcuts, investigation workflow states, remediation progress tracking, per-service health feeds, reliability score per service, MTTR/impact/trust trend dashboards, and investor-ready reports. By the end, power users navigate with keyboard shortcuts and leadership has analytics dashboards.

## Tasks

- [x] Wave 4 environment validation. Run all test suites across ALL components (investigator pytest + ruff + mypy, UI pytest, operator cargo test). Verify all tests from Waves 1-3 still pass. If any failures exist, fix them and commit as `fix: wave-4 pre-flight`. Report total test counts per component. Make all decisions autonomously.
  - **Results**: All tests pass. Fixed 2 issues committed as `fix: wave-4 pre-flight`:
    - Added mypy overrides for github/gitlab/kubernetes missing type stubs (14 errors → 0)
    - Moved test-only `RoutingConfig` import to `#[cfg(test)]` module (1 warning → 0)
  - **Test counts**:
    - Investigator pytest: **1013 passed**, 3 skipped | ruff: **clean** | mypy: **clean** (41 files)
    - UI pytest: **1788 passed**
    - Operator cargo test: **531 passed**
  - **Total: 3,332 tests passing across all components**

- [x] Create story 7-1 spec (Command Palette & Keyboard Shortcuts) using the `/bmad-bmm-create-story` skill from Epic 7 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.
  - **Results**: Story 7-1 spec created and implemented. UI-only feature: command palette (Cmd+K/Ctrl+K) with ARIA combobox, real-time client-side filtering of 17 commands, chord keyboard shortcuts (g+i, g+s, etc.), ? help overlay. 23 new tests. All 3,355 tests passing (1,013 investigator + 1,811 UI + 531 operator).

- [ ] Code review story 7-1 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `7-1-command-palette-keyboard-shortcuts: done`. Update story file status to `done`. Commit as `7-1 done`. Make all decisions autonomously.

- [ ] Create story 7-2 spec (Investigation Workflow States) using the `/bmad-bmm-create-story` skill from Epic 7 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 7-2 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `7-2-investigation-workflow-states: done`. Update story file status to `done`. Commit as `7-2 done`. Make all decisions autonomously.

- [ ] Create story 7-3 spec (Remediation Progress Tracking) using the `/bmad-bmm-create-story` skill from Epic 7 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 7-3 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `7-3-remediation-progress-tracking: done`. Update story file status to `done`. Commit as `7-3 done`. Make all decisions autonomously.

- [ ] Create story 7-4 spec (Per-Service Health Feeds) using the `/bmad-bmm-create-story` skill from Epic 7 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 7-4 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `7-4-per-service-health-feeds: done`. Update story file status to `done`. Commit as `7-4 done`. Make all decisions autonomously.

- [ ] QA checkpoint: Run the full test suite across ALL components (investigator pytest + ruff + mypy, UI pytest, operator cargo test). Report total test counts per component. If any regressions exist, fix them and commit as `fix: QA checkpoint regression`. Make all decisions autonomously.

- [ ] Create story 7-5 spec (Reliability Score per Service) using the `/bmad-bmm-create-story` skill from Epic 7 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 7-5 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `7-5-reliability-score-per-service: done`. Update story file status to `done`. Commit as `7-5 done`. Make all decisions autonomously.

- [ ] Create story 7-6 spec (MTTR, Impact & Trust Trend Dashboards) using the `/bmad-bmm-create-story` skill from Epic 7 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 7-6 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `7-6-mttr-impact-trust-trend-dashboards: done`. Update story file status to `done`. Commit as `7-6 done`. Make all decisions autonomously.

- [ ] Create story 7-7 spec (Investor-Ready Reports) using the `/bmad-bmm-create-story` skill from Epic 7 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 7-7 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `7-7-investor-ready-reports: done`. Update story file status to `done`. Commit as `7-7 done`. Make all decisions autonomously.

- [ ] Run Epic 7 retrospective using the `/bmad-bmm-retrospective` skill. Create retrospective file at `_bmad-output/implementation-artifacts/epic-7-retro-2026-MM-DD.md` (use today's date). Update sprint-status.yaml: `epic-7: done`, `epic-7-retrospective: done`. Commit as `epic-7 retrospective done`. Make all decisions autonomously.
