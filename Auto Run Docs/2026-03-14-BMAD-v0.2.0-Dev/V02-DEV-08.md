# Phase 08: Epic 8 — Investor Demo Platform (Wave 4) + Final Validation

This phase completes Wave 4 and v0.2.0 by implementing Epic 8 (4 stories): chaotic demo application deployment, configurable fault injection, full lifecycle demonstration, and scripted repeatable demo scenarios. After the epic retrospective, a final release validation confirms all 56 stories, all 8 epics, and the full test suite are green. By the end, Diana can run polished investor demos and v0.2.0 is complete.

## Tasks

- [x] Create story 8-1 spec (Chaotic Demo Application Deployment) using the `/bmad-bmm-create-story` skill from Epic 8 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.
  - Story spec created at `_bmad-output/implementation-artifacts/8-1-chaotic-demo-application-deployment.md`
  - Implementation complete: multi-role Flask demo server, K8s manifests, ServiceLevel CRDs, Makefile, 133 demo tests passing
  - No regressions: operator 538, investigator 1013, UI 2023 — total 3,707 tests passing

- [ ] Code review story 8-1 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `8-1-chaotic-demo-application-deployment: done`. Update story file status to `done`. Commit as `8-1 done`. Make all decisions autonomously.

- [ ] Create story 8-2 spec (Configurable Fault Injection) using the `/bmad-bmm-create-story` skill from Epic 8 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 8-2 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `8-2-configurable-fault-injection: done`. Update story file status to `done`. Commit as `8-2 done`. Make all decisions autonomously.

- [ ] QA checkpoint: Run the full test suite across ALL components (investigator pytest + ruff + mypy, UI pytest, operator cargo test). Report total test counts per component. If any regressions exist, fix them and commit as `fix: QA checkpoint regression`. Make all decisions autonomously.

- [ ] Create story 8-3 spec (Full Lifecycle Demonstration) using the `/bmad-bmm-create-story` skill from Epic 8 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 8-3 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `8-3-full-lifecycle-demonstration: done`. Update story file status to `done`. Commit as `8-3 done`. Make all decisions autonomously.

- [ ] Create story 8-4 spec (Scripted Repeatable Demo Scenarios) using the `/bmad-bmm-create-story` skill from Epic 8 in `_bmad-output/planning-artifacts/epics.md`. Then implement it using the `/bmad-bmm-dev-story` skill. Update sprint-status.yaml. Make all decisions autonomously.

- [ ] Code review story 8-4 using the `/bmad-bmm-code-review` skill. Auto-fix all issues found. Run all test suites across all components. Update sprint-status.yaml: `8-4-scripted-repeatable-demo-scenarios: done`. Update story file status to `done`. Commit as `8-4 done`. Make all decisions autonomously.

- [ ] Run Epic 8 retrospective using the `/bmad-bmm-retrospective` skill. Create retrospective file at `_bmad-output/implementation-artifacts/epic-8-retro-2026-MM-DD.md` (use today's date). Update sprint-status.yaml: `epic-8: done`, `epic-8-retrospective: done`. Commit as `epic-8 retrospective done`. Make all decisions autonomously.

- [ ] Final v0.2.0 release validation. Run the full test suite across ALL components. Verify sprint-status.yaml shows all 56 stories as `done`, all 8 epics as `done`, and all 8 retrospectives as `done`. Check that all story files in `_bmad-output/implementation-artifacts/` have status `done`. Create a final sprint summary at `_bmad-output/implementation-artifacts/final-sprint-summary-v0.2.0-2026-MM-DD.md` (use today's date) covering: aggregate test counts, epic-by-epic breakdown, FR coverage (63/63), code review effectiveness, and velocity analysis. Commit as `v0.2.0 complete`. Make all decisions autonomously.
